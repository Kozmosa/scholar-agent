- 2026-04-13 01:35 `terminal-bench-task3-api-routes`：按 TDD 先扩展 `tests/test_api_terminal.py`，新增终端 session 鉴权与 create/delete round-trip 失败用例，并确认在终端路由缺失时按预期失败；随后最小实现 terminal router、root 与 `/v1` 路由挂载、routes 导出及会话响应序列化辅助，保持 ttyd 生命周期仍为占位行为；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_terminal.py -q` 首次按预期失败（404/缺少 `ainrf.api.routes.terminal`），实现后复跑通过 3 个用例。
- 2026-04-13 02:05 `terminal-bench-task4-ttyd-lifecycle`：按 TDD 先在 `tests/test_terminal_ttyd.py` 增加 ttyd provider 启停生命周期失败用例，并在 `tests/test_api_terminal.py` 增加路由必须读取 `api_config` 终端配置并调用真实 lifecycle helper 的失败用例；确认初次运行因缺少 `start_ttyd_session` 导入而按预期失败后，最小实现 `src/ainrf/terminal/ttyd.py` 的 `Popen`/`SIGTERM` 生命周期与 `src/ainrf/api/routes/terminal.py` 的真实 POST/DELETE 接线；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_terminal_ttyd.py /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_terminal.py -q` 先按预期失败，随后复跑 8 个用例全部通过。
