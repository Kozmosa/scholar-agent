- 2026-04-13 01:35 `terminal-bench-task3-api-routes`：按 TDD 先扩展 `tests/test_api_terminal.py`，新增终端 session 鉴权与 create/delete round-trip 失败用例，并确认在终端路由缺失时按预期失败；随后最小实现 terminal router、root 与 `/v1` 路由挂载、routes 导出及会话响应序列化辅助，保持 ttyd 生命周期仍为占位行为；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_terminal.py -q` 首次按预期失败（404/缺少 `ainrf.api.routes.terminal`），实现后复跑通过 3 个用例。
- 2026-04-13 16:09 `terminal-bench-task5-auth-openapi-docs`：按 TDD 先扩展 `tests/test_api_auth.py` 与 `tests/test_api_v1_routes.py`，补充 `/terminal/session` 鉴权断言和 `/terminal/session`、`/v1/terminal/session` OpenAPI 注册断言，并先运行定向 pytest 确认新增断言经历失败后再收敛为最小测试修正；随后更新 `src/ainrf/README.md` 的 Terminal Bench MVP 说明，记录 GET/POST/DELETE `/terminal/session` 及其单一 ttyd browser terminal 语义，并去掉 auth/v1 路由测试中的重复 OpenAPI 断言与对 `ainrf.api.routes.tasks` 物理删除的实现耦合检查；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_auth.py /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_v1_routes.py -q` 首次按预期失败（错误地把已注册 `/terminal/session` 当作 404），修正后复跑 8 个用例全部通过。
- 2026-04-13 16:39 `terminal-bench-task6-frontend-contract`：按 TDD 新增 `frontend/src/terminal-contract.test.ts` 作为前端终端 session 合约检查，先运行前端构建确认缺失 `TerminalSession` 类型与 terminal session API/mock helper 时出现预期 TypeScript 失败；随后最小补充 `TerminalSessionStatus`、`TerminalSession`、`get/create/deleteTerminalSession` 与对应 mock 状态机，并将 `provider`/`target_kind` 收紧为 `'ttyd'` / `'daemon-host'` 字面量类型以对齐后端契约；验证结果为 `npm --prefix /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/frontend run build` 首次按预期报缺少导出，修正后 `npm --prefix /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/frontend exec -- tsc -p /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/frontend/tsconfig.app.json --noEmit` 通过。
- 2026-04-13 17:25 `terminal-bench-mvp`：补充 `src/ainrf/README.md` 的 Terminal Bench MVP 收口说明，明确本地 `ttyd --version` 依赖检查与当前 runtime API 列表（`/health`、`/v1/health`、`/terminal/session` 与 `/v1/terminal/session` 的 GET/POST/DELETE）；随后执行计划要求的最终验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_ttyd.py tests/test_api_terminal.py tests/test_api_auth.py tests/test_api_health.py tests/test_api_v1_routes.py tests/test_cli.py tests/test_server.py tests/test_execution_ssh.py -q` 通过 42 个用例，`python -m compileall src/ainrf` 通过，`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过但仍打印既有 Tailwind/lightningcss `@theme`/`@tailwind` warning；追加 working-directory 回归修复后，再用 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest tests/test_terminal_ttyd.py tests/test_api_terminal.py -q` 验证终端相关 8 个用例通过，并在宿主机确认 `ttyd --version` 为 1.6.3；真实 smoke 方面，使用显式 `AINRF_API_KEY_HASHES` 启动前台服务后，`GET /health`、`GET /terminal/session`、`POST /terminal/session`、`DELETE /terminal/session` 均返回 200，说明本地终端 session API 已可完成基础启动/停止闭环；但本次会话中仍未实际打开浏览器 iframe 执行 `pwd`/`whoami`/`claude --version`，因此浏览器内交互 smoke 仍待人工补完。
- 2026-04-13 18:05 `terminal-bench-bugfix-working-dir`：按 TDD 先在 `tests/test_terminal_ttyd.py` 新增缺失 `.ainrf` 工作目录时仍可启动 ttyd session 的回归用例，并用 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_terminal_ttyd.py -q` 确认其先因 `resolve(strict=True)` 抛出 `FileNotFoundError` 而失败；随后最小修改 `src/ainrf/terminal/ttyd.py`，在启动前创建并标准化 working directory，再复跑上述定向测试与 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_terminal.py -q`，结果 8 个终端相关用例全部通过。
- 2026-04-13 23:20 `onboarding-non-tty-guard`：收敛首轮 onboarding 的交互性检查，仅按 stdin/stdout 是否为 TTY 判定是否允许进入 prompt，移除“存在 Click context 即提前返回”的分支，避免非 TTY CLI 上下文误落入 `typer.prompt`；同步调整 `tests/test_cli.py`，为交互式 bootstrap 显式伪造 TTY 流，并新增非 TTY stdin/stdout 组合断言以验证会在 prompt 前抛出 `typer.BadParameter`；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-ad4aa78a run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-ad4aa78a/tests/test_cli.py` 通过 19 个用例，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-ad4aa78a run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-ad4aa78a/src/ainrf/onboarding.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-ad4aa78a/tests/test_cli.py` 通过。
- 2026-04-13 23:35 `onboarding-task4-explicit-command`：按 TDD 先在 `tests/test_cli.py` 增加显式 `ainrf onboard` 写入配置与已有配置覆盖确认两个用例，并运行定向 pytest 确认在命令缺失时以 exit code 2 按预期失败；随后最小修改 `src/ainrf/onboarding.py` 增加 `run_onboarding(state_root)` 覆盖确认入口，并在 `src/ainrf/cli.py` 暴露 `onboard` 命令，保持现有可选 container profile 与非 TTY onboarding 行为不变；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py::test_onboard_command_writes_config /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py::test_onboard_command_prompts_before_overwrite -q` 先失败后通过。
- 2026-04-13 23:55 `onboarding-task4-overwrite-invalid-config`：修复显式 `ainrf onboard` 在已有损坏 JSON 配置上选择覆盖时仍会先读取旧配置导致失败的问题，为 `onboard_state_root()` 增加仅在覆盖路径下重置 payload 的分支，并在 `tests/test_cli.py` 增加无效现有配置确认覆盖后可成功重建配置的回归用例；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py` 通过 23 个用例，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/src/ainrf/onboarding.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py` 通过。
- 2026-04-14 00:10 `onboarding-task4-remove-serve-bootstrap`：移除 `ainrf serve` 通过 `_ensure_api_key_hashes_configured()` 隐式触发 onboarding 的路径，改为仅接受环境变量或现有配置中的 `api_key_hashes`，缺失时提示先执行显式 `ainrf onboard`；同步删去 serve 自动 bootstrap 测试，改为断言缺失配置时直接失败，同时保留显式 `ainrf onboard`、覆盖确认、可选 container profile 与非 TTY onboarding 用例；验证结果见本批次定向 `pytest` 与 `ruff check`，均通过。
- 2026-04-14 00:36 `onboarding-task4-validation-errors`：最小修复显式 `ainrf onboard` 与 runtime 配置解析的剩余 Task 4 问题：在 `run_onboarding()` 入口补上非 TTY 交互守卫，避免显式 onboarding 在非交互上下文落入 prompt；同时为 `load_runtime_config()` 捕获损坏 JSON 并统一抛出 `typer.BadParameter`，让 `ainrf serve` 在读取畸形 `config.json` 时输出干净的 CLI 校验错误。同步调整 `tests/test_cli.py`，覆盖显式 onboard 的非 TTY 拒绝、malformed JSON 的加载校验，以及 serve 路径上的用户可读失败；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py -q` 24 个用例通过，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/src/ainrf/onboarding.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-a56032ad/tests/test_cli.py` 通过。
- 2026-04-14 00:50 `onboarding-task5-serve-auto-onboarding`：按 TDD 先在 `tests/test_cli.py` 替换/新增 `serve` 首次运行自动 onboarding 与无交互输入快速失败用例，并先运行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py -q` 确认新增断言失败；随后最小修改 `src/ainrf/cli.py`，仅在本地配置缺失且 `AINRF_API_KEY_HASHES` 未设置时让 `serve` 复用 onboarding helper 自动写入配置，缺失交互输入时输出明确提示引导改用 `ainrf onboard`，同时保持 env 覆盖、显式 `ainrf onboard` 与 daemon/runtime 启动流不变；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py -q` 26 个用例通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/cli.py tests/test_cli.py` 通过。
- 2026-04-13 23:58 `onboarding-task5-tty-guard-reuse`: 最小修复 `src/ainrf/cli.py` 的 `serve` 首次运行自动 onboarding 守卫，改为复用 `ensure_interactive_onboarding_available()` 统一判定非 TTY/非交互场景，并在缺失本地配置且无环境变量哈希时继续输出明确引导 `ainrf onboard` 的失败提示；同步调整 `tests/test_cli.py`，把非交互回归从 `stdin is None` 改为真实非 TTY stdin 路径，并断言不会落入 prompt/confirm。验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py -q` 26 个用例通过，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/src/ainrf/cli.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py` 通过。
- 2026-04-14 01:09 `onboarding-task5-validation-unmasking`：最小收窄 `src/ainrf/cli.py` 中 `serve` 首次运行自动 onboarding 的异常改写范围，仅在 `ensure_interactive_onboarding_available()` 判定非交互场景时输出通用引导 `Run \`ainrf onboard\` interactively`，不再吞掉 `onboard_state_root()` 内部抛出的真实校验错误；同步在 `tests/test_cli.py` 新增回归用例，伪造交互式 TTY 但让 API key prompt 返回空白，确认 `API key cannot be empty.` 会直接暴露且不会被通用提示覆盖。验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py -q` 26 个用例通过，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/src/ainrf/cli.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py` 通过。
- 2026-04-14 01:01 `onboarding-task5-remove-dead-sys-plumbing`：最小清理 Task 5 遗留的无效模块级 plumbing，删除 `src/ainrf/cli.py` 中的 `onboarding.sys = sys` 赋值以及 `src/ainrf/onboarding.py` 中未使用的 `import sys`，保持现有首轮 `serve` 自动 onboarding 与非 TTY 守卫行为不变；验证结果为 `UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py -q` 26 个用例通过，`UV_CACHE_DIR=/tmp/uv-cache uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75 run ruff check /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/src/ainrf/cli.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/src/ainrf/onboarding.py /home/xuyang/code/scholar-agent/.claude/worktrees/agent-add33a75/tests/test_cli.py` 通过。
