- 2026-04-13 00:45 `cleanup-final-gap-fix`：修正 cleanup completion audit 暴露的最后一致性缺口，将前端 `SystemHealth` / mock / `HealthStatusBar` / dashboard 文案改为匹配后端 `HealthResponse` 契约（`status`、`state_root`、`container_configured`、`container_health`、`detail`），并把 `src/ainrf/cli.py` 顶层 help 文案从旧 orchestration scaffold 改为 daemon-oriented runtime CLI；验证结果为 `cd frontend && npx tsc --noEmit --project tsconfig.app.json` 通过，`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py::test_help_shows_commands -q` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execution_ssh.py tests/test_server.py tests/test_cli.py tests/test_api_health.py tests/test_api_auth.py tests/test_api_v1_routes.py -q` 通过 34 个用例，且 `python -m compileall src/ainrf` 通过。
- 2026-04-13 01:05 `terminal-bench-task1-schemas-config`：按 TDD 先新增终端 bench 首批失败测试，仅覆盖 `GET /terminal/session` 的 idle/ttyd/daemon-host 契约断言，并确认在尚未接线路由时按预期失败（404）；随后补充后端终端默认配置字段和完整终端会话 schema/status，保持路由层留给后续任务实现；验证结果为 `uv --project /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment run pytest /home/xuyang/code/scholar-agent/.claude/worktrees/ainrf-cleanup-realignment/tests/test_api_terminal.py::test_terminal_session_starts_idle -q` 首次按预期失败（`/terminal/session` 为 404），修改后再次运行同一命令仍按预期失败，失败原因保持为路由未实现。