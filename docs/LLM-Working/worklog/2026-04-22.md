2026-04-22 11:32 changelog：完成 Environments / Containers spec 第二层闭环实现，后端新增 `projects/default/environment-refs` 与 `/v1/...` API、environment-aware terminal ensure 与 remote SSH PTY 启动、environment-aware single-active workspace/code-server manager 与 `/code/session` 控制接口，前端同步接入 project default 优先的共享环境选择、Containers 项目绑定/override 编辑、Terminal/Workspace 按 selected environment ensure，以及 password-auth 环境的 terminal 支持和 workspace unsupported 展示；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`npm run lint`、`npm run test:run`、`npm run build` 通过，`pytest` 仍会提示 `src/ainrf/api/routes/code.py` catch-all 代理在 OpenAPI 里生成 duplicate operation id warning，但不影响接口行为与测试通过；关联提交：`6c6a396 feat: close environment binding control plane`
2026-04-22 12:06 changelog：完成内网 WebUI 一键启动入口，新增 `scripts/webui.sh` 与可测试的 `scripts/run_webui.py`，默认自动准备 `./.ainrf/`、生成并持久化 `./.ainrf/webui.env` service key、把 hash 合并进 `./.ainrf/config.json`、联动启动 `ainrf serve` 与前端 dev/preview，并在子进程退出或 `SIGINT` 时统一清理；前端同步抽出共享 Vite 代理配置，为 `/api`、`/code`、`/terminal` 统一注入 `X-API-Key` 且保留原始 Host，浏览器端不再依赖启动时注入 `VITE_AINRF_API_KEY`，同时补齐 terminal WebUI origin 合同测试，并更新 `src/ainrf/README.md` 与 `docs/ainrf/index.md` 的推荐启动方式说明；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check scripts/run_webui.py tests/test_run_webui.py tests/test_api_terminal.py`、`cd frontend && npm run lint && npm run test:run && npm run build` 通过；已知保留项：`pytest` 仍提示 `src/ainrf/api/routes/code.py` catch-all 代理的 duplicate operation id warning，另 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests scripts` 仍会命中仓库中若干与本次改动无关的既有未格式化文件，故本次仅保证新增/修改的 Python 文件已格式化。
2026-04-22 12:26 changelog：完成 `feat/webterm-keepalive` Slice 1，后端把单全局 PTY terminal 升级为 `SessionManager + TmuxAdapter + TerminalAttachmentBroker` 组合，新增 SQLite 持久化的 `user_environment_bindings` / `user_session_pairs`、tmux-backed personal session ensure/detach/reset、`/terminal/session/reset` 与 `/terminal/attachments/{attachment_id}/ws`，并在 FastAPI lifespan 中接入 startup reconcile / attachment cleanup；前端同步把 Terminal Bench 改为 `Attach / Detach / Reset session` 语义，补齐 personal session / attachment contract 字段、running-but-detached 自动 reattach、显式 detach 抑制自动重连、reset 后切换新 ws URL，同时更新 README、Dashboard surface 列表与 keepalive 说明；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`npm run lint`、`npm run test:run`、`npm run build` 通过，`pytest` 仍有 `src/ainrf/api/routes/code.py` duplicate operation id warning，前端测试仍有 jsdom `HTMLCanvasElement.getContext()` not implemented warning，`vite build` 仍提示主 bundle 超过 500 kB，但不影响本次 Slice 1 功能与验证结果
2026-04-22 13:23 changelog：完成 `feat/webterm-keepalive` 与最新 `master` 的合并冲突收敛，保留上游内网 WebUI 一键启动与 Vite proxy 头注入链路，并把 terminal 相关合同测试与 API 测试更新为 WebUI origin + tmux attachment 语义，确保 keepalive personal session 实现与 `/terminal/attachments/{attachment_id}/ws` 路径兼容上游代理转发；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`cd frontend && npm run lint && npm run test:run && npm run build` 通过，保留项仍为 `src/ainrf/api/routes/code.py` duplicate operation id warning、前端测试中的 jsdom canvas warning 与 `vite build` bundle-size warning
2026-04-22 13:23 changelog：完成 Mode1 的 top2 research ideas 实验方案与首轮 pilot 验证，新增 `test/Mode1/idea-discovery/refine-logs/EXPERIMENT_PLAN.md`、`EXPERIMENT_TRACKER.md`、`PILOT_RESULTS.md` 及时间戳快照文件，并实现 `test/Mode1/experiments/run_top2_pilots.py` 用于 DynHG-LLM / FedST-LLM 的合成机制级验证；验证：在 `cuda:0` 上执行 `python3 test/Mode1/experiments/run_top2_pilots.py --device cuda:0 --seeds 7 11` 成功，DynHG-LLM 当前较 pairwise 基线退化 `10.53%` 但通信比率降至 `0.01`，FedST-LLM 较 FedAvg 改善 `5.63%` 且与 centralized gap 为 `-0.84%`，据此判定 top1 需先修正 mask collapse，top2 可继续加深验证。
2026-04-22 13:36 changelog：完成 Mode1 top2 ideas 第二轮 pilot 重构与追溯笔记沉淀，更新 `test/Mode1/experiments/run_top2_pilots.py` 以加入 DynHG 的受控参与/残差超图混合、FedST 的 client-level hypergraph context 与 FedProx 基线，并新增 `docs/LLM-Working/refactoring-plan/mode1-top2-validation-notes.md`、`test/Mode1/idea-discovery/refine-logs/2026-04-22_pilot_results_round2.md` 记录 milestones 与关键发现；验证：执行 `python3 test/Mode1/experiments/run_top2_pilots.py --device cuda:0 --seeds 7 11 --tag top2_pilot_results_round2` 成功，DynHG-LLM 较 pairwise 基线进一步退化至 `-33.58%` 且通信比率回升到 `0.17`，FedST-LLM 较 FedAvg 改善 `15.02%`、较 FedProx 改善 `1.45%`，但相对 centralized gap 仍为 `174.87%`，据此将 DynHG 标记为架构级阻塞、FedST 标记为可继续沿 context interface 深挖但暂不宣称接近 centralized。
2026-04-22 13:45 changelog：完成 Mode1 top2 ideas 第三轮 pilot 重构与耦合强度扫描，进一步更新 `test/Mode1/experiments/run_top2_pilots.py`，将 DynHG 改为 pairwise 主路径 + non-overlapping hyperedge correction，并为 FedST 引入显式 pair-context / hyper-context split 与 `0.2/0.3/0.4` coupling sweep，同时补充 `2026-04-22_pilot_results_round3.md` 和追溯笔记中的 M6 记录；验证：执行 `python3 test/Mode1/experiments/run_top2_pilots.py --device cuda:0 --seeds 7 11 --tag top2_pilot_results_round3 --coupling-sweep` 成功，DynHG-LLM 相对 pairwise 仅退化 `1.20%` 且通信比率为 `0.17`，说明已从严重失败恢复到接近基线但仍无正增益；FedST-LLM 在 moderate coupling (`0.3`) 下较 FedAvg 改善 `6.11%`、较 FedProx 改善 `2.55%`，与 centralized gap 收敛到 `2.54%`，据此将 DynHG 维持 HOLD、FedST 标记为 GO(scoped)，可准备更高置信度验证与 real-data bridge 规划。
2026-04-22 14:07 changelog：完成 `feat/webterm-keepalive-s2` Slice 2，后端在现有 tmux personal-session 基线上补齐 agent session / task window 能力，扩展 `SessionManager`、`TmuxAdapter`、`TerminalAttachmentBroker`，新增基于同一 `terminal_state.sqlite3` 的 `managed_tasks` / `task_terminal_bindings` 持久化、`/terminal/session-pairs` 与 `/tasks`/`/tasks/{id}`/`/tasks/{id}/cancel`/`/tasks/{id}/terminal`/`/tasks/{id}/terminal/open`（含 `/v1/...` 镜像）接口，并在 startup reconcile 中恢复 agent/task 状态；前端同步新增独立 `/tasks` 路由与侧边导航、task create/list/detail 页面、observe-only terminal 打开链路和 `TerminalSessionConsole` readonly 模式，保持 `/terminal` 继续只负责 personal session；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`cd frontend && npm run lint`、`cd frontend && npm run test:run`、`cd frontend && npm run build` 通过；保留 warnings：`src/ainrf/api/routes/code.py` duplicate operation id、前端测试中的 jsdom `HTMLCanvasElement.getContext()` warning、`vite build` bundle-size warning
2026-04-22 14:48 changelog：完成 Mode1 top2 ideas 的 go/hold 收敛与 bridge 准备沉淀，延续第三轮结果将 DynHG 标记为 HOLD、FedST 标记为 GO(scoped)，补充 `test/Mode1/experiments/bridge_plan.md` 规划 PEMS03 / METR-LA 的 client partition 与 real-data bridge 最小执行集，并将追溯笔记中的 M7、实验 tracker 状态与 manifest 同步更新；验证：复核 `experiments/results/top2_pilot_results_round3.json` / `.md`、`docs/LLM-Working/refactoring-plan/mode1-top2-validation-notes.md`、`test/Mode1/idea-discovery/refine-logs/2026-04-22_experiment_tracker.md` 一致，确认当前结论为 DynHG 暂停深入、FedST 进入多 seed 与 real-data bridge 准备阶段。
2026-04-22 15:01 changelog：完成 FedST 的 5-seed 置信度验证与 bridge 工具实现，新增 `test/Mode1/experiments/prepare_fedst_bridge.py` 与 `experiments/results/bridge/` 下的 `client_partitions.json`、`client_graph.json`、`client_hypergraph.json`、`locality_partition_placeholder.json`，并产出 `test/Mode1/idea-discovery/refine-logs/2026-04-22_fedst_multiseed_round5.md` 记录多 seed 结果；验证：执行 `python3 test/Mode1/experiments/prepare_fedst_bridge.py --dataset-name synthetic-traffic-bridge --num-sensors 12 --clients 4 --seed 7` 与 `python3 test/Mode1/experiments/run_top2_pilots.py --device cuda:0 --seeds 3 7 11 19 23 --tag fedst_round4_seeds5` 成功，FedST 在 moderate coupling (`0.3`) 下较 FedAvg 改善 `6.62%`、较 FedProx 改善 `3.94%`，与 centralized gap 为 `2.86%`，据此把 FedST 的 scoped go 结论提升为更高置信度，并将下一步聚焦为 real-data smoke test（Bridge-M1）。
2026-04-22 15:41 changelog：完成 `feat/webterm-keepalive-s3` 严格版落地，后端为 terminal/task REST 统一接入 `X-AINRF-User-Id`、将 `SessionManager` 改为 app-user namespace + legacy daemon binding 首次领取迁移、补齐 task runtime wrapper/control（embedded python wrapper + runtime socket + pause/resume/status ack）、新增 `task_takeover_leases` 与 takeover/release API/状态模型，并让 task attachment 严格区分 `observe` / `write`；前端同步新增浏览器级 app user id 持久化与 REST 头注入、把 `/tasks` 收敛为 discovery/control 面板、把 `/terminal` 扩展为 task attach 模式并显示 owner/mode/release 状态，相关 TS 类型、mock 与合同测试全部更新；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src/ainrf/terminal/models.py src/ainrf/terminal/attachments.py src/ainrf/terminal/tmux.py src/ainrf/terminal/sessions.py src/ainrf/tasks/models.py src/ainrf/tasks/runtime.py src/ainrf/tasks/service.py src/ainrf/api/app.py src/ainrf/api/schemas.py src/ainrf/api/routes/terminal.py src/ainrf/api/routes/tasks.py tests/test_terminal_sessions.py tests/test_api_terminal.py tests/test_api_tasks.py tests/test_task_runtime.py tests/test_api_v1_routes.py`、`cd frontend && npm run lint && npm run test:run && npm run build` 通过；保留 warnings：`pytest` 仍提示 `src/ainrf/api/routes/code.py` catch-all 代理的 duplicate operation id，前端测试仍有 jsdom `HTMLCanvasElement.getContext()` warning，`vite build` 仍提示主 bundle 超过 500 kB，但不影响本次 Slice 3 功能与验证结果。
2026-04-22 16:50 changelog：完成 `feat/webterm-keepalive` Slice 4，后端在 `TaskManager`/`TmuxAdapter` 中补齐 task takeover 的 `grace_pending` 5 秒断线重连宽限、服务重启后的 active lease -> startup grace reconcile、expired grace sweep 自动 release+resume、completed/failed/cancelled task window 的 60 分钟保留后 kill+archive，以及 final window 缺失时直接 archive；同时把 `/terminal/attachments/{attachment_id}/ws` 关闭路径接入 write task attachment disconnect 回调，前端 `TerminalPage` 在 task-mode websocket disconnect 后清空本地 attachment guard 以允许同 intent 重新 attach，`TasksPage` 对 archived task 禁用 `Open terminal` / `Take over`，并补齐后端生命周期、API、websocket、tmux kill-window 与前端交互测试。验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src/ainrf/tasks/models.py src/ainrf/tasks/service.py src/ainrf/terminal/tmux.py src/ainrf/api/app.py src/ainrf/api/routes/terminal.py tests/test_api_tasks.py tests/test_terminal_pty.py tests/test_terminal_sessions.py tests/test_task_manager.py`、`cd frontend && npm run lint`、`cd frontend && npm run test:run`、`cd frontend && npm run build` 通过；保留 warnings：`pytest` 仍提示 `src/ainrf/api/routes/code.py` catch-all 代理的 duplicate operation id，前端测试仍有 jsdom `HTMLCanvasElement.getContext()` warning，`vite build` 仍提示主 bundle 超过 500 kB，但不影响本次 Slice 4 功能与验证结果。
2026-04-22 17:05 changelog：完成 `feat/webterm-keepalive-s4` 的 spec 收尾文档同步，更新 `src/ainrf/README.md` 以反映 Slice 1-4 已落地的 personal session / managed task terminal / takeover grace / archive 生命周期、`/terminal/session-pairs` 与 `/tasks`/`/tasks/{id}/terminal/*`（含 `/v1/...`）接口、`X-AINRF-User-Id` 要求，以及 `terminal_state.sqlite3` 中新增的 `managed_tasks`、`task_terminal_bindings`、`task_takeover_leases`；同时补充 `docs/ainrf/index.md` 对当前 WebUI/API 控制面能力的概述，去除 README 中“旧 task 路径未注册”和“两张主表”这类已过时表述。验证：`UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，并复核 `src/ainrf/README.md` / `docs/ainrf/index.md` 中的 terminal/task 描述与当前实现一致。
2026-04-22 17:15 changelog：完成 `feat/webterm-keepalive-s4` 的 warning 收口，后端把 `src/ainrf/api/routes/code.py` 的 catch-all code-server 代理路由改为不进入 OpenAPI schema，从而消除 `duplicate operation id` warning；前端在 `frontend/src/test/setup.ts` 增加通用 `HTMLCanvasElement.getContext()` 2D context stub，消除 jsdom canvas warning，并在 `frontend/src/App.tsx` 和 `frontend/vite.config.ts` 引入 route-level lazy loading + vendor manual chunk 拆分，把原先的单大 bundle 拆为 `app-vendor` / `terminal-vendor` 等 chunk，从而消除 Vite 的 `>500 kB` chunk-size warning。验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py tests/test_api_terminal.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`cd frontend && npm run lint`、`cd frontend && npm run test:run`、`cd frontend && npm run build` 通过；本轮原有三个 warning 均已消失。
