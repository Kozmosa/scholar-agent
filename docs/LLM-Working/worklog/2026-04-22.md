2026-04-22 11:32 changelog：完成 Environments / Containers spec 第二层闭环实现，后端新增 `projects/default/environment-refs` 与 `/v1/...` API、environment-aware terminal ensure 与 remote SSH PTY 启动、environment-aware single-active workspace/code-server manager 与 `/code/session` 控制接口，前端同步接入 project default 优先的共享环境选择、Containers 项目绑定/override 编辑、Terminal/Workspace 按 selected environment ensure，以及 password-auth 环境的 terminal 支持和 workspace unsupported 展示；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`npm run lint`、`npm run test:run`、`npm run build` 通过，`pytest` 仍会提示 `src/ainrf/api/routes/code.py` catch-all 代理在 OpenAPI 里生成 duplicate operation id warning，但不影响接口行为与测试通过；关联提交：`6c6a396 feat: close environment binding control plane`
2026-04-22 12:06 changelog：完成内网 WebUI 一键启动入口，新增 `scripts/webui.sh` 与可测试的 `scripts/run_webui.py`，默认自动准备 `./.ainrf/`、生成并持久化 `./.ainrf/webui.env` service key、把 hash 合并进 `./.ainrf/config.json`、联动启动 `ainrf serve` 与前端 dev/preview，并在子进程退出或 `SIGINT` 时统一清理；前端同步抽出共享 Vite 代理配置，为 `/api`、`/code`、`/terminal` 统一注入 `X-API-Key` 且保留原始 Host，浏览器端不再依赖启动时注入 `VITE_AINRF_API_KEY`，同时补齐 terminal WebUI origin 合同测试，并更新 `src/ainrf/README.md` 与 `docs/ainrf/index.md` 的推荐启动方式说明；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests scripts`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check scripts/run_webui.py tests/test_run_webui.py tests/test_api_terminal.py`、`cd frontend && npm run lint && npm run test:run && npm run build` 通过；已知保留项：`pytest` 仍提示 `src/ainrf/api/routes/code.py` catch-all 代理的 duplicate operation id warning，另 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests scripts` 仍会命中仓库中若干与本次改动无关的既有未格式化文件，故本次仅保证新增/修改的 Python 文件已格式化。
