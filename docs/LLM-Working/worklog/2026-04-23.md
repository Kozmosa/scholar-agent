2026-04-23 00:14 changelog：完成 `2026-04-14-webui-style-loading-fix-design` 现状核对，复查前端已保留 `@tailwindcss/vite` 集成、`verify:styles` 构建后 CSS 片段回归校验与 2026-04-14 的原始 worklog 验证记录，并补跑当前仓库的样式回归与 lint 以确认该修复仍然成立；验证：执行 `cd frontend && npm run verify:styles` 成功，构建产物生成并输出 `Verified Tailwind CSS output in index-C5ktkGPB.css`，随后执行 `cd frontend && npm run lint` 成功；结论：实现与可复现回归验证已完成，但仓库内未找到“浏览器人工视觉复查已完成”的单独明确记录，仅有前端服务启动与“供人工验收/自动化验证受限”的日志证据。
2026-04-23 00:46 changelog：完成活跃 spec 集合清理，按当前实现现状删除已完成使命的 `2026-04-14-webui-style-loading-fix-design.md`、`2026-04-06-ref-repos-submodules-design.md` 与 `2026-04-13-ainrf-onboard-design.md`，并把 `docs/superpowers/specs/2026-04-22-implemented-runtime-surface-archive.md` 扩展为 `Runtime / Infra` 归档入口，补充 `.gitmodules` 与 2026-04-13 onboarding worklog 为事实层参考，同时确认 `2026-04-22-docs-context-isolation-design.md` 仍未完成、继续保留在活跃 specs；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，且复查 `docs/superpowers/specs/` 现仅剩归档页与 `docs-context-isolation` 两份文档。
2026-04-23 02:10 changelog：完成 `research-dev` skills package 设计 spec，明确单 package、多子 skill 的结构，约定 `skills/skills-index.md` 仅作为与 package 源码同目录维护的维护者索引而不属于 installable skill surface，并收敛 `using-research-dev`、`project-init`、`env-bootstrap`、`git-workflow`、`commit-style`、`repo-ops` 的职责边界，同时定义 research-project runnable scaffold 的目录合同、`uv`/`uv run` 工具链入口、默认 branch/worktree 命名与基于 `npx skills` 的安装维护契约；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。
2026-04-23 03:05 changelog：完成基于 Mode1 现有数据、文献与失败经验的新一轮 idea discovery，围绕真实交通联邦预测重新构造并验证一批新想法，新增 `test/Mode1/experiments/run_new_idea_batch.py`、`test/Mode1/idea-stage/IDEA_REPORT.md` 与 `IDEA_CANDIDATES.md`；验证：执行 `python3 test/Mode1/experiments/run_new_idea_batch.py --metr-h5 /home/xuyang/code/scholar-agent/test/Mode1/metr-la.h5 --bay-h5 /home/xuyang/code/scholar-agent/test/Mode1/pems-bay.h5 --clients 4 --seed 7` 成功，得到 4 个已验证 idea，其中 locality-aware partitioned FL 在 METR-LA 上较随机划分 FedProx 提升 `24.65%`、在 PEMS-BAY 上提升 `3.54%`，FedPer-Region personalization 与 Sparse boundary-message FedPer 分别得到 `-6.47%` 与 `-20.01%` 的负结果，据此确认已满足“3+ verified ideas（成功失败都算）”目标，并将后续最优方向收敛为 partition-first federated traffic forecasting。
2026-04-23 09:08 changelog：完成 code-server 接入 spec 完成度审计，从 git 历史复核 `2026-04-14-code-server-managed-embedding-design.md` 后，对照当前 `EnvironmentCodeServerManager`、`/code/status`、`/code/session`、`/code/` 代理、前端 `CodeServerCard` 与相关测试，确认受管 workspace browser 主体能力已经落地并且后续已演进为 environment-aware 单活动 workspace manager；同时识别出与原始第一版设计不完全一致的两点：当前不是 `ainrf serve` 启动即自动拉起 code-server，而是通过 `/code/session` 按 environment 显式 ensure，且 `/code/` 代理仍只覆盖 HTTP 转发、未见 websocket/upgrade 专用实现或测试，因此更准确结论是“核心接入已实现，但并非逐字满足原始第一版 spec”；验证：复核 `src/ainrf/code_server.py`、`src/ainrf/api/routes/code.py`、`src/ainrf/api/app.py`、`frontend/src/components/code-server/CodeServerCard.tsx`、`tests/test_code_server.py`、`tests/test_api_code.py`、`tests/test_api_v1_routes.py`、`src/ainrf/README.md`、`docs/ainrf/index.md` 与 `docs/LLM-Working/worklog/2026-04-14.md`、`docs/LLM-Working/worklog/2026-04-22.md` 一致。
2026-04-23 09:25 changelog：完成可行主线的正式推进收敛，基于已验证的 locality-aware partitioned FL 新方向新增 `test/Mode1/idea-stage/PARTITION_FIRST_PLAN.md` 与 `PARTITION_FIRST_TRACKER.md`，将后续工作从旧的 FedST family 切换为 partition-first federated traffic forecasting 主线；验证：复核 `test/Mode1/idea-stage/IDEA_REPORT.md` 中 4 个 verified ideas 与 `run_new_idea_batch.py` 输出一致，并确认 locality-aware partitioned FL 是唯一跨 `METR-LA` / `PEMS-BAY` 保持正信号的方向，可作为下一阶段 `research-refine + experiment-plan` 的正式输入。
2026-04-23 09:27 changelog：完成 AINRF 中心化清理的第一批身份层调整，新增仓库根 `README.md`，把 AINRF 前后端、CLI、API 与 WebUI 明确设为默认产品叙事，并同步修订 `PROJECT_BASIS.md` 的项目目标、目录职责和文档入口规则，明确 `docs/` 与 `ref-repos/` 主要承担参考语料与历史追溯职责，不再把 notes 视为仓库中心；验证：执行 `git diff --check` 通过，并复核 `README.md` 与 `PROJECT_BASIS.md` 的目录/目标描述相互一致。
2026-04-23 09:28 changelog：完成 AINRF 中心化清理的第二批打包层调整，把 `pyproject.toml` 的分发包名从 `scholar-agent-notes` 调整为 `ainrf`，同步更新包描述并让包元数据直接引用新的仓库根 `README.md`，使安装面与 CLI 命名保持一致；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ainrf --help` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv build --wheel` 成功并产出 `dist/ainrf-0.1.0-py3-none-any.whl`。
2026-04-23 09:30 changelog：完成 AINRF 中心化清理的第三批文档入口调整，重写 `docs/index.md` 与 `docs/ainrf/index.md` 的默认叙事，使 AINRF 前后端、CLI、API 与 WebUI 成为 `docs/` 的主入口，并同步修订 `docs/framework/index.md`，把框架页收敛为设计/架构与历史追溯入口而不是产品使用首页；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，复核新的默认阅读顺序已切换为 `ainrf -> 当前产品定位 -> 设计/架构索引`。
2026-04-23 09:32 changelog：完成 AINRF 中心化清理的第四批收尾同步，修订 `src/ainrf/README.md` 以明确 `src/ainrf` 是 AINRF 的后端/runtime 主实现并与 `frontend/` 共同构成产品面，替换 `frontend/README.md` 的 Vite 模板为项目化说明，同时把 `mkdocs.yml` 的站点标题与描述改为 AINRF docs/reference 语义，并同步纳入因包名调整引起的 `uv.lock` 本地包条目更新；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ainrf --help` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。
2026-04-23 10:05 changelog：完成 partition-first 主线的多 seed 稳定性验证，新增 `test/Mode1/experiments/run_partition_first_multiseed.py` 与 `test/Mode1/idea-stage/PARTITION_FIRST_RESULTS.md`，系统评估 locality-aware partitioned FL 在 `METR-LA` / `PEMS-BAY` 上相对随机划分 FedProx 的种子稳定性；验证：执行 `python3 test/Mode1/experiments/run_partition_first_multiseed.py --metr-h5 /home/xuyang/code/scholar-agent/test/Mode1/metr-la.h5 --bay-h5 /home/xuyang/code/scholar-agent/test/Mode1/pems-bay.h5 --clients 4 --seeds 3 7 11 19 23` 成功，METR-LA 平均提升 `6.57%` 但仅 `2/5` seeds 为正，PEMS-BAY 平均提升 `3.07%` 且 `3/5` seeds 为正，据此将 partition-first 方向从“已确认可行主线”下调为“当前最优但仍需稳定化的候选主线”，后续优先级改为先修正 partition heuristic 的鲁棒性，再决定是否进入正式 paper 主结果路线。
2026-04-23 11:05 changelog：完成 `Task Harness v1` 设计 spec，围绕 web harness 启动与监控 Claude Code 会话这一最小闭环，明确 task 必须显式绑定 `workspace` 与 `environment`、采用 `global harness/system -> workspace -> environment -> task_profile -> task input` 的五层 prompt composition 合同，并定义 v1 的生命周期、后端模块、REST + SSE 输出 contract、失败语义与 deferred 范围；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。
2026-04-23 11:13 changelog：完成 `perf/enhance-pty` PTY attachment 输出链路增强，在 `src/ainrf/terminal/pty.py` 引入增量 UTF-8 decoder 与 reader/queue/watermark 内部常量，在 `src/ainrf/api/routes/terminal.py` 把 attachment websocket 输出改为有界字节队列、按高低水位暂停/恢复 PTY reader，并在 EOF/EIO/EBADF 哨兵后 flush decoder，避免中文多字节跨 chunk 被替换且避免输出缓存无界增长；同时在 `tests/test_terminal_pty.py` 新增 decoder、多字节边界、EOF flush、软背压暂停/恢复和既有 websocket 行为回归测试。验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_pty.py tests/test_terminal_sessions.py tests/test_api_terminal.py tests/test_api_tasks.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 成功；仓库级 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 仍因未触碰的既有文件 `src/ainrf/api/routes/environments.py`、`src/ainrf/api/routes/projects.py`、`src/ainrf/code_server.py`、`src/ainrf/runtime/config.py`、`tests/test_api_environments.py` 与 `tests/test_code_server.py` 失败，但本次改动文件执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src/ainrf/api/routes/terminal.py src/ainrf/terminal/pty.py tests/test_terminal_pty.py` 成功；关联提交：`1ee6763 fix: harden terminal attachment pty output`
2026-04-23 11:23 changelog：完成 `perf/enhance-pty` 与 `origin/master` 的冲突消解，合入上游 AINRF 中心化清理改动并仅在 `docs/LLM-Working/worklog/2026-04-23.md` 手工解决内容冲突，保留 09:27–09:32 的上游工作记录与 11:13 的 PTY 增强记录的时间顺序，使分支恢复与 `origin/master` 的可合并状态；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_pty.py tests/test_terminal_sessions.py tests/test_api_terminal.py tests/test_api_tasks.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ainrf --help` 成功；关联提交：`242f779 merge: sync origin/master into perf/enhance-pty`
2026-04-23 12:15 changelog：完成 `Task Harness v1` 全量替换，在后端新增 `workspace` seed registry 与 `task_harness` 持久化运行时，扩展 environment 增加 `task_harness_profile` 并把 `/tasks` 改为 `queued | starting | running | succeeded | failed` 状态机，落地 SQLite 输出回放、SSE 流、binding/prompt/runtime snapshot 文件与 `/workspaces` 路由；同时前端重写 Tasks 页为 workspace+environment 选取、detail/replay/SSE 视图，Terminal 页退回 personal terminal bench，Containers 页补 task harness profile 编辑区，并同步更新 mock、类型与回归测试，移除旧 task terminal/takeover/cancel API 的契约覆盖。验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 成功（125 passed），执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 成功，执行 `cd frontend && npm ci` 成功，执行 `cd frontend && npm run lint` 成功，执行 `cd frontend && npm run build` 成功。
2026-04-23 12:18 changelog：补记 `Task Harness v1` 批次的提交信息，前述 12:15 完成的实现批次已整理为可审阅提交，便于后续 PR 直接对应同一工作切片；验证：未追加新实现验证，本条仅回填提交元数据；关联提交：`0dafa28 feat: implement task harness v1`
2026-04-23 12:55 changelog：完成 AINRF settings page baseline 设计 spec，明确 `/settings` 第一版只做浏览器本地持久化，收敛为 `General Preferences` 与当前固定 project 下按 environment 分组的 `Project Defaults` 两层，冻结默认首页、终端字号/自动换行、项目默认环境与任务默认标题/命令/工作目录为 must-have，并明确不纳入 token、后端设置 API、多项目切换与多浏览器同步；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。
2026-04-23 12:57 changelog：完成 WebUI 联调入口瘦身，删除 `scripts/run_webui.py` 与对应重型测试，把 `scripts/webui.sh` 改为直接导出会话级 `AINRF_WEBUI_API_KEY` 与 `AINRF_API_KEY_HASHES`、设置默认 `UV_CACHE_DIR=/tmp/uv-cache`，并最小化同时拉起 `uv run ainrf serve` 与前端 dev/preview；同步更新 `src/ainrf/README.md`、`docs/ainrf/index.md`、`frontend/README.md` 的启动说明，并新增直接运行 shell 脚本的 `tests/test_webui_sh.py` 覆盖默认模式与 `preview --backend-public` 参数。验证：执行 `bash -n scripts/webui.sh` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_webui_sh.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check tests/test_webui_sh.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/test_webui_sh.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check tests/test_webui_sh.py` 成功；补充说明：仓库级 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 仍会命中 `ref-repos/` 下既有类型问题，因此本次仅对受影响测试文件做针对性静态检查。
2026-04-23 13:01 changelog：完成 `ty check` 噪声收敛，在 `pyproject.toml` 新增 `[tool.ty.src] exclude = ["ref-repos"]`，把只读参考仓库从仓库级类型检查目标中排除，避免 `ref-repos/` 既有第三方类型问题持续污染主仓库验证结果；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 成功。
2026-04-23 13:19 changelog：完成 Web terminal reattach regression 修复，在 `src/ainrf/terminal/tmux.py` 内新增 tmux-safe session target 规范化函数，并统一用于 `has-session`、`new-session`、`kill-session`、`set-option`、`attach-session`、`new-window` 与 `list-windows`，从而保持 `SessionManager`、API schema、前端展示和 SQLite 中的原始 namespaced `session_name` contract 不变；同时在 `tests/test_terminal_sessions.py` 把 adapter 命令构造断言切到真实 namespaced session 名，并新增模拟 tmux 将 `:` 收敛为 `_` 的后端回归测试，覆盖首次 attach 后再次 ensure 不再触发 `duplicate session`。验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_sessions.py tests/test_api_terminal.py tests/test_terminal_pty.py` 成功（39 passed），执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 成功（120 passed），执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 成功。
2026-04-23 15:29 changelog：完成旧版目录叙事清理，针对仍把 `src/ainrf/artifacts/`、`src/ainrf/gates/`、`src/ainrf/events/` 写成当前源码树现状的历史文档做收口：在 `docs/LLM-Working/refactoring-plan/contracts-to-current-ainrf-gap-plan.md` 明确其为 pre-`task_harness/workspaces/terminal` 的 gap-analysis，并把“当前已具备/缺失”收紧为“当时”；在 `docs/LLM-Working/refactoring-plan/architecture-baseline-plan.md` 把现状基线改为 `task_harness / tasks / workspaces / terminal / environments / code_server`；同时为 `docs/framework/v1-implementation-status.md` 与 `docs/framework/v1-roadmap.md` 补充历史锚点说明，避免再把已移除路径误读为现行目录 contract。验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。
