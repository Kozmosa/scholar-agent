2026-04-23 00:14 changelog：完成 `2026-04-14-webui-style-loading-fix-design` 现状核对，复查前端已保留 `@tailwindcss/vite` 集成、`verify:styles` 构建后 CSS 片段回归校验与 2026-04-14 的原始 worklog 验证记录，并补跑当前仓库的样式回归与 lint 以确认该修复仍然成立；验证：执行 `cd frontend && npm run verify:styles` 成功，构建产物生成并输出 `Verified Tailwind CSS output in index-C5ktkGPB.css`，随后执行 `cd frontend && npm run lint` 成功；结论：实现与可复现回归验证已完成，但仓库内未找到“浏览器人工视觉复查已完成”的单独明确记录，仅有前端服务启动与“供人工验收/自动化验证受限”的日志证据。
2026-04-23 00:46 changelog：完成活跃 spec 集合清理，按当前实现现状删除已完成使命的 `2026-04-14-webui-style-loading-fix-design.md`、`2026-04-06-ref-repos-submodules-design.md` 与 `2026-04-13-ainrf-onboard-design.md`，并把 `docs/superpowers/specs/2026-04-22-implemented-runtime-surface-archive.md` 扩展为 `Runtime / Infra` 归档入口，补充 `.gitmodules` 与 2026-04-13 onboarding worklog 为事实层参考，同时确认 `2026-04-22-docs-context-isolation-design.md` 仍未完成、继续保留在活跃 specs；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，且复查 `docs/superpowers/specs/` 现仅剩归档页与 `docs-context-isolation` 两份文档。
2026-04-23 11:13 changelog：完成 `perf/enhance-pty` PTY attachment 输出链路增强，在 `src/ainrf/terminal/pty.py` 引入增量 UTF-8 decoder 与 reader/queue/watermark 内部常量，在 `src/ainrf/api/routes/terminal.py` 把 attachment websocket 输出改为有界字节队列、按高低水位暂停/恢复 PTY reader，并在 EOF/EIO/EBADF 哨兵后 flush decoder，避免中文多字节跨 chunk 被替换且避免输出缓存无界增长；同时在 `tests/test_terminal_pty.py` 新增 decoder、多字节边界、EOF flush、软背压暂停/恢复和既有 websocket 行为回归测试。验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_pty.py tests/test_terminal_sessions.py tests/test_api_terminal.py tests/test_api_tasks.py` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 成功，执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 成功；仓库级 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 仍因未触碰的既有文件 `src/ainrf/api/routes/environments.py`、`src/ainrf/api/routes/projects.py`、`src/ainrf/code_server.py`、`src/ainrf/runtime/config.py`、`tests/test_api_environments.py` 与 `tests/test_code_server.py` 失败，但本次改动文件执行 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src/ainrf/api/routes/terminal.py src/ainrf/terminal/pty.py tests/test_terminal_pty.py` 成功；关联提交：`1ee6763 fix: harden terminal attachment pty output`
