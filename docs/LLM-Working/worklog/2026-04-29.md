# 2026-04-29

2026-04-29 00:25 changelog：完成 runtime readiness 前端提示、onboarding readiness 记录与 code-server tmux 停止清理补强；工作区浏览器页读取 `/health` 的 runtime readiness 并在打开托管浏览器前显示缺失依赖提示，mock health 与前端类型同步补齐 `startup_cwd`、默认工作区和 readiness 字段；onboarding 写入 `runtime_readiness` 快照，便于配置文件记录 tmux、uv、code-server 初始状态；tmux 启动的 code-server 停止命令增加 bounded SIGTERM 等待与 `kill -9` 兜底，减少退出后残留进程风险。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py tests/test_code_server.py tests/test_api_auth.py tests/test_api_tasks.py tests/test_api_environments.py tests/test_code_server_installer.py tests/test_server.py tests/test_terminal_pty.py tests/test_api_terminal.py tests/test_runtime_paths.py tests/test_runtime_readiness.py -q` 通过（111 passed, 1 skipped），`npm --prefix frontend test -- --run src/pages/WorkspaceBrowserPage.test.tsx src/pages/SettingsPage.test.tsx --reporter=dot` 通过（6 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过，`npm --prefix frontend run build` 通过。

2026-04-29 00:33 changelog：完成 daemon stop 命令切片；新增 `stop_server_daemon()` 读取 daemon PID 文件、对仍存活进程复用进程组终止逻辑并移除 PID 文件，CLI 增加 `ainrf stop` 入口，使用与 daemon mode 一致的默认 `state_root/runtime/ainrf-api.pid`，让后台服务可通过显式命令清理而不是只依赖健康检查失败路径。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py tests/test_server.py -q` 通过（34 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-29 00:48 changelog：完成启动 readiness preflight 与终端断连语义测试切片；FastAPI lifespan 启动时记录 localhost runtime readiness snapshot，`/health` 优先复用启动期快照，避免每次 health 请求重复探测并让启动状态可被 app state 统一引用；补充终端 attachment 断连回归测试，确认 Ctrl-D/浏览器 WebSocket 断开只关闭 attach bridge runtime，不杀 durable personal tmux session，后续 reattach 仍看到 personal session 处于运行态。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py tests/test_terminal_sessions.py tests/test_cli.py tests/test_server.py -q` 通过（67 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-29 01:02 changelog：完成 onboarding runtime readiness 指引切片；`ainrf onboard` 在写入 runtime readiness snapshot 后会立即输出依赖状态，全部就绪时提示 ready，存在缺失依赖时按 dependency 名称列出 tmux、uv、code-server 等 remediation detail，避免用户只在后续工作区浏览器失败时才看到依赖问题。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py tests/test_runtime_readiness.py -q` 通过（33 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-29 01:10 changelog：完成 code-server readiness 安装引导补强；runtime readiness 在缺失 code-server 时的 remediation detail 现在同时指向 Settings 安装入口和 `POST /environments/env-localhost/install-code-server` 后端安装端点，使 onboarding、health 和 workspace browser 中复用的提示都能引导用户走现有 installer 闭环。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_runtime_readiness.py tests/test_cli.py tests/test_api_v1_routes.py -q` 通过（41 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。
