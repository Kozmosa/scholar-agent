2026-04-14 00:00 changelog：完成 AINRF 用法文档设计说明，明确将在 docs/ainrf/index.md 以单页方式记录当前真实可用命令面、文档预览入口与 AINRF 服务入口；验证：已核对 CLAUDE.md、PROJECT_BASIS.md、src/ainrf/cli.py 与 src/ainrf/server.py 的现有命令表面，设计与仓库当前实现一致。
2026-04-14 02:30 changelog：完成 AINRF 使用文档单页，新增 `docs/ainrf/index.md`，整理当前真实可用的 CLI、文档预览与构建入口，明确文档站点预览与 API 服务的边界；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，并顺手移除了历史 worklog 中一个失效 wikilink 的双括号以恢复站点构建。
2026-04-14 12:59 changelog：完成 Task 2 的 code-server supervisor 最小闭环，新增 `src/ainrf/code_server.py` 与 `tests/test_code_server.py`，先以测试驱动补齐命令构造、未配置/路径缺失降级、启动探测成功与失败回收语义，保持 code-server 作为受管子进程且未涉及 API 路由；验证：依次执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server.py -q`（初次因缺少模块失败，补最小实现后通过）、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server.py::test_supervisor_marks_ready_after_successful_probe tests/test_code_server.py::test_supervisor_degrades_when_probe_fails -q`（实现前失败，实现后通过）、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server.py -q`（最终 5 项全部通过）。
