2026-04-14 00:00 changelog：完成 AINRF 用法文档设计说明，明确将在 docs/ainrf/index.md 以单页方式记录当前真实可用命令面、文档预览入口与 AINRF 服务入口；验证：已核对 CLAUDE.md、PROJECT_BASIS.md、src/ainrf/cli.py 与 src/ainrf/server.py 的现有命令表面，设计与仓库当前实现一致。
2026-04-14 02:30 changelog：完成 AINRF 使用文档单页，新增 `docs/ainrf/index.md`，整理当前真实可用的 CLI、文档预览与构建入口，明确文档站点预览与 API 服务的边界；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功，并顺手移除了历史 worklog 中一个失效 wikilink 的双括号以恢复站点构建。
2026-04-14 14:45 changelog：完成 code-server 受管嵌入第一版实现收口与运行时文档补充，当前 runtime 已具备 supervisor、`/code/status` 状态接口、`/code/` 代理入口与前端 workspace browser 面板，并保持 code-server 缺失或 workspace 不可用时 API 降级运行；验证：执行 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server.py tests/test_api_code.py tests/test_api_v1_routes.py tests/test_cli.py tests/test_server.py -q`、`npm --prefix frontend run build`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`，其中 pytest / frontend build / ruff check 通过，`ruff format --check src tests` 失败并暴露既有格式漂移，需在后续单独收敛。
