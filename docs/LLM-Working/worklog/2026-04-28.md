2026-04-28 11:55 changelog：完成环境探测 SSH 不可用时的个人 tmux 会话回退切片；从最新 `origin/master` 创建 `feat/environment-detect-tmux-fallback` worktree/branch，将环境 detect 从手动 stub 改为真实 SSH-first probe，并在 SSH 连接不可用时通过请求用户的 personal tmux session 执行 marker 包裹的 bounded probe command、capture-pane 收集输出后生成 `latest_detection`，同时保持 `/health` 仍为 SSH-only 语义；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest` 通过（155 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-28 13:16 changelog：完成设置页 code-server 安装器切片；新增环境配置字段 `code_server_path`，后端提供 latest Linux amd64 code-server release 解析、幂等安装脚本、SSH-first / personal tmux fallback 安装服务和 `/environments/{id}/install-code-server` API，code-server runtime 启动改为优先使用环境记录中的安装路径；前端设置页在共享环境选择器下新增安装卡片，支持显示已记录路径、触发安装、刷新环境缓存，并同步 mock/API/types/i18n；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest` 通过（166 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过，`npm --prefix frontend run test:run` 通过（84 passed），`npm --prefix frontend run build` 通过；`npm --prefix frontend run lint` 仍命中既有 `WorkspaceManagerCard.tsx` React hooks 规则问题，非本切片新增。

2026-04-28 15:02 changelog：完成 Localhost 环境探测与 code-server 安装 tmux fallback 修复；移除 `env-localhost` seed 探测的硬编码失败短路，使其复用现有 SSH-first / personal tmux fallback probe，并将 code-server 安装器 personal tmux fallback 的 session bootstrap 异常统一包装为 `CodeServerInstallError`，避免安装 Localhost 时泄漏为 500 `Unexpected environment error`；补充 Localhost detect fallback、Localhost installer fallback 与 installer fallback setup error 回归测试；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_environments.py tests/test_code_server_installer.py -q` 通过（22 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-28 15:29 changelog：完成 Localhost fallback 重试与前端提示修正；将 SSH command 执行前的连接建立限制到 `connect_timeout`，为 Localhost 探测设置 3 次短连接尝试后进入 personal tmux fallback，并在 Localhost 默认工作目录不存在时让 personal tmux 会话回退到 daemon state root 启动，避免 `/workspace/projects` 缺失导致 fallback 失败；前端容器页接入 ToastProvider，探测使用 personal tmux fallback 或失败时弹出 toast 提示；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_environments.py tests/test_code_server_installer.py tests/test_execution_ssh.py tests/test_terminal_sessions.py -q` 通过（57 passed），`npm --prefix frontend test -- --run src/pages/ContainersPage.test.tsx --reporter=dot` 通过（10 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过。

2026-04-28 15:53 changelog：完成 Localhost personal tmux fallback marker 修复；修正 bounded tmux probe 的 marker 输出命令，从未正确换行的 `printf %s\\n` 改为带格式串引用的 `printf '%s\\n'` / `printf '%s:%s\\n'`，避免 capture-pane 中 marker 与命令输出粘连导致解析超时；补充本地与远程 bounded command wrapper 断言，并用真实 Localhost 探测脚本确认 3 次 SSH 短尝试后成功通过 personal tmux fallback 返回 `Detection completed for localhost via personal tmux fallback.`；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_terminal_sessions.py tests/test_api_environments.py tests/test_execution_ssh.py -q` 通过（49 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`npm --prefix frontend test -- --run src/pages/ContainersPage.test.tsx --reporter=dot` 通过（10 passed）。

2026-04-28 20:43 changelog：完成 Localhost 个人 tmux 直连策略切片；新增共享 Localhost 判定，让 `env-localhost` 探测与 code-server 安装不再先尝试 SSH，而是直接使用请求用户的 personal tmux session，缺少用户/session manager 时返回类型化冲突错误；code-server Localhost runtime 从本地 `subprocess.Popen` 改为 personal tmux 中启动并通过 runtime pid/log 文件管理启停，远程环境保持 SSH-first 与 tunnel 行为不变；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_environments.py -k "localhost or install_code_server or detect" -q` 通过（10 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server_installer.py -k "localhost or personal_tmux" -q` 通过（3 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server.py -q` 通过（9 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_code.py -q` 通过（5 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-28 22:08 changelog：完成 code-server 安装输出解析与设置页终端输出切片；安装脚本输出增加 `__AINRF_CODE_SERVER_INSTALL_RESULT__` 哨兵并按行扫描解析，避免 personal tmux 捕获到下载进度或 shell prompt 后把安装结果误判为 `invalid install output`；Localhost / personal tmux 安装结果新增 terminal attachment 元数据并通过 `/environments/{id}/install-code-server` 返回，设置页安装卡片在成功响应后用现有 `TerminalSessionConsole` 展示安装终端输出，同时保留环境缓存刷新与安装路径展示；后续复测发现 tmux `capture-pane` 会按终端宽度折行，导致长安装结果哨兵行被截断，已改为 `capture-pane -J -p` 保留逻辑行并补充折行回归测试；再次复测定位到 GitHub latest release API 触发 403 rate limit，已将 HTTP 获取失败包装为 typed `CodeServerInstallError` 并支持 `AINRF_CODE_SERVER_VERSION` 指定版本直连 release tarball URL 以绕过 latest API；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_code_server_installer.py tests/test_api_environments.py -k "install_code_server or release_asset or localhost or detect" -q` 通过（21 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过；此前 `npm --prefix frontend test -- --run src/pages/SettingsPage.test.tsx --reporter=dot` 通过（5 passed），`npm --prefix frontend run build` 通过。

2026-04-28 22:27 changelog：完成默认工作空间目录修正；将 seed `workspace-default` 的 `default_workdir` 从 AINRF 启动目录本身改为启动目录下的 `workspace/default`，初始化时自动创建该目录，并同步任务 API 本地/远程路径断言，确保任务 harness 的 resolved workdir 与远程 project_dir 都指向该默认工作空间目录；验证结果：先运行定向红测确认旧实现仍解析到仓库根目录，随后 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py::test_task_harness_routes_create_list_detail_output_and_workspaces tests/test_api_tasks.py::test_task_harness_remote_path_runs_without_readiness_precheck -q` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py -q` 通过（4 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_harness_service.py -q` 通过（3 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-28 23:39 changelog：完成默认工作区、运行时 readiness 与进程清理重构首批切片；新增 runtime path resolver，把 AINRF 启动目录记录到 `ApiConfig.startup_cwd` 并统一导出 `workspace/default` 默认路径，`WorkspaceRegistryService` 与 `env-localhost` seed 改为由 app 启动注入该本地默认目录，避免继续依赖 `/workspace/projects` 或 state root；环境探测与 code-server SSH 安装对缺少显式 remote workdir 的环境改为类型化失败，防止把本地路径误传给远程；`/health` 增加 runtime readiness payload，报告 tmux、uv、code-server 是否可用并给出安装提示；daemon 健康探测失败时的清理改为对进程组发送 SIGTERM 并在超时后 SIGKILL，降低残留子进程风险。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_auth.py tests/test_api_tasks.py tests/test_api_environments.py tests/test_code_server.py tests/test_code_server_installer.py tests/test_server.py tests/test_terminal_pty.py tests/test_api_terminal.py tests/test_runtime_paths.py tests/test_runtime_readiness.py -q` 通过（81 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。
