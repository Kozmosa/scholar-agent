2026-04-28 11:55 changelog：完成环境探测 SSH 不可用时的个人 tmux 会话回退切片；从最新 `origin/master` 创建 `feat/environment-detect-tmux-fallback` worktree/branch，将环境 detect 从手动 stub 改为真实 SSH-first probe，并在 SSH 连接不可用时通过请求用户的 personal tmux session 执行 marker 包裹的 bounded probe command、capture-pane 收集输出后生成 `latest_detection`，同时保持 `/health` 仍为 SSH-only 语义；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest` 通过（155 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-28 13:16 changelog：完成设置页 code-server 安装器切片；新增环境配置字段 `code_server_path`，后端提供 latest Linux amd64 code-server release 解析、幂等安装脚本、SSH-first / personal tmux fallback 安装服务和 `/environments/{id}/install-code-server` API，code-server runtime 启动改为优先使用环境记录中的安装路径；前端设置页在共享环境选择器下新增安装卡片，支持显示已记录路径、触发安装、刷新环境缓存，并同步 mock/API/types/i18n；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest` 通过（166 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过，`npm --prefix frontend run test:run` 通过（84 passed），`npm --prefix frontend run build` 通过；`npm --prefix frontend run lint` 仍命中既有 `WorkspaceManagerCard.tsx` React hooks 规则问题，非本切片新增。
