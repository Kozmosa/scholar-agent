2026-04-24 00:00 changelog：完成 `task-harness-no-readiness` worktree 提交前整理，清理本分支验证遗留的 `.venv`、`node_modules`、`dist`、pytest/ruff cache 与各类 `__pycache__` 生成目录，保留本次功能改动与 2026-04-23 的实现 worklog，并将提交相关文件整理到可直接审阅/提交的状态；验证：执行 `git status --short --ignored=matching` 确认仅剩源码与 worklog 变更、忽略产物已清空，执行 `git diff --stat` 复核改动范围与预期一致。

2026-04-24 14:16 changelog：完成已合并与过时分支清理，先 `git fetch --prune origin` 复核候选，再删除本地已合并且未占用分支 11 个、已合并且干净的 worktree 分支 2 个、7 天以上无 upstream 且干净的过时 worktree 分支 20 个，并删除 `origin` 上已合并到 `origin/master` 的主题分支 15 个；保留 19 个仍有未提交改动的 worktree 分支。未合并过时分支删除前审计（格式：分支=SHA, master-only/branch-only）：`worktree-agent-a2067742=f060ba6,108/1`，`worktree-agent-a28b748b=ff5d1b3,108/1`，`worktree-agent-a2ef36d3=8da6dc4,93/2`，`worktree-agent-a3234ebe=59506b7,93/8`，`worktree-agent-a382a506=2e3c8e8,108/4`，`worktree-agent-a4a61594=8ebc568,71/2`，`worktree-agent-a54d0d3e=2ef0f4e,93/10`，`worktree-agent-a56032ad=4781cc1,108/7`，`worktree-agent-a6894ac5=eed5ec5,108/1`，`worktree-agent-a6cffa3b=381d496,108/1`，`worktree-agent-a75adf04=b0fea47,93/13`，`worktree-agent-a8fa6d61=b1890fa,71/1`，`worktree-agent-aaaf3fcc=0b5834b,108/3`，`worktree-agent-ac0da43d=0ee13db,93/2`，`worktree-agent-ad4aa78a=fbdb6e0,108/2`，`worktree-agent-ad8c60fb=63e272d,93/12`，`worktree-agent-add33a75=a3e8774,108/11`，`worktree-agent-ae66a178=cc4a5ed,93/4`，`worktree-containers-page=59c4d66,71/4`，`worktree-task2-ttyd-proxy-auth-retry=0468029,69/2`；验证：重新 `git fetch --prune origin` 后，`git branch -r` 仅剩 `origin/HEAD` 与 `origin/master`，`git branch -r --no-merged origin/master` 为空，`git worktree list --porcelain` 只保留主 worktree 与 dirty worktree，`git status --short --branch` 显示 `master...origin/master` 且除本 worklog 外无产品代码变更；本批次未产生提交。

2026-04-24 14:55 changelog：完成 Claude Code 仓库协作说明修订，在 `CLAUDE.md` 新增 Git maintenance 约束，记录 worktree/branch cleanup 的 audit-first 命令序列与 dirty worktree 保留规则；验证：已应用用户批准的单行长期说明，未运行测试（仅文档约束变更）。

2026-04-24 00:20 changelog：完成 Baseline Contract Audit，核对 API v1 OpenAPI 契约、terminal attachment 关闭时缺失可选 `app.state.task_manager` 的容错行为，以及 Task Harness 重启后未完成任务转失败的既有约束；补充 `tests/test_api_v1_routes.py` 以同时固定 `/v1/tasks/{task_id}` 旧式 cancel/terminal 路由仍不存在，其余运行时代码保持不变；验证：`UV_CACHE_DIR=/tmp/uv-cache uv --directory "/home/xuyang/code/scholar-agent/.worktrees/refactor-baseline-contract-audit" run pytest tests/test_api_v1_routes.py tests/test_terminal_pty.py tests/test_task_harness_service.py`、`UV_CACHE_DIR=/tmp/uv-cache uv --directory "/home/xuyang/code/scholar-agent/.worktrees/refactor-baseline-contract-audit" run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv --directory "/home/xuyang/code/scholar-agent/.worktrees/refactor-baseline-contract-audit" run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv --directory "/home/xuyang/code/scholar-agent/.worktrees/refactor-baseline-contract-audit" run ruff format --check src tests` 全部通过。

2026-04-24 00:00 changelog：完成 `task-2-guidance-alignment`，将 `CLAUDE.md` 与 `AGENTS.md` 的仓库叙事统一到 `PROJECT_BASIS.md` 当前口径，明确 `frontend/` 与 `src/ainrf/` 是 AINRF 的核心产品实现面，`docs/` 与 `ref-repos/` 是长期知识与参考资产，并移除对 AINRF 仅为 scaffold/未来计划的过时描述；验证：执行 `git -C "/home/xuyang/code/scholar-agent/.worktrees/refactor-docs-policy-alignment" diff --check` 通过，并复核 `git -C "/home/xuyang/code/scholar-agent/.worktrees/refactor-docs-policy-alignment" diff -- CLAUDE.md AGENTS.md` 确认改动仅为文档/策略对齐。

2026-04-24 16:24 changelog：完成 `Task 3 docs build contract tests`，新增 `tests/test_build_html_notes.py` 覆盖 frontmatter 解析、Obsidian callout 转换、wikilink 与 Markdown 链接处理、反向链接追加等既有构建契约，并通过动态导入脚本复用当前实现而未改动 `scripts/build_html_notes.py` 语义；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_build_html_notes.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check scripts tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check scripts tests` 均通过。

2026-04-24 17:05 changelog：完成 `Task 3 reviewer follow-up`，按 review 要求把空 callout 用例从精确字符串快照改为语义断言，并新增基于临时目录与路径常量 monkeypatch 的集成测试，直接执行 `build_processed_docs()` 验证 Markdown 组合输出、反向链接生成与非 Markdown 资产复制契约，保持 `scripts/build_html_notes.py` 生产语义不变；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_build_html_notes.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check scripts tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check scripts tests`。

2026-04-24 17:05 changelog：完成 TasksPage 职责拆分，提取任务输出合并辅助函数到 `frontend/src/pages/tasks/output.ts`、将 SSE 回放与重连逻辑抽到 `frontend/src/pages/tasks/useTaskOutputStream.ts`，并拆出 `TaskCreateForm`、`TaskList`、`TaskDetail` 三个纯 UI 组件，保持 5 秒轮询、SSE URL 形状、空标题提交、创建后选中与列表刷新语义不变；补充并保留 `TasksPage` 测试覆盖重复/乱序 seq 处理、SSE gap replay 与断线重连补回。验证：执行 `cd frontend && npm run lint`、`cd frontend && npm run test:run -- TasksPage`、`cd frontend && npm run build` 全部通过；本批次未产生提交。
