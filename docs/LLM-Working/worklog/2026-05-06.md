## 2026-05-06 00:40 CST — Task 5: SkillInjectionService

- 实现 `src/ainrf/skills/injection.py`:
  - `SkillInjectionService(skill_root)` 构造函数
  - `generate_ainrf(workdir, selected_skills, task_settings_override)`:
    - 使用 `SkillLoader.load_all_from_root` 加载全部技能
    - DFS 递归解析依赖，检测循环依赖和缺失依赖
    - 按 `InjectMode` 过滤：`DISABLED` 跳过，`PROMPT_ONLY` 仅复制目录，`AUTO` 复制并合并 `settings_fragment`
    - 使用 `deep_merge_settings` 按顺序合并 base settings、AUTO 技能片段、task override
    - 使用 `resolve_env_placeholders` 解析环境变量占位符
    - 写入 `.ainrf/settings.json` 和 `.ainrf/tool-manifest.json`
  - `sync_to_claude(workdir)`:
    - 调用 `sync_ainrf_to_claude` 同步技能目录
    - `shutil.copy2` 复制 settings.json 到 `.claude/`
- 编写 `tests/skills/test_injection.py` 共 10 个测试，覆盖目录创建、settings 合并、task override、prompt_only 过滤、disabled 跳过、依赖解析、循环依赖、缺失依赖、tool-manifest 内容、sync_to_claude
- 运行 `uv run pytest tests/skills/` 全部 31 个测试通过
- `ruff check` 和 `ruff format` 无问题
- Commit: `5096203`

## 2026-05-06 11:00 CST — Task 8: Integrate skill injection into local task launcher and harness service

- 修改 `src/ainrf/task_harness/launcher.py`:
  - `build_local_launcher()` 在构建命令前检查 `Path(working_directory) / ".ainrf" / "settings.json"` 是否存在
  - 存在则优先使用该路径，否则回退到传入的 `settings_path`
- 修改 `src/ainrf/task_harness/service.py`:
  - `TaskHarnessService.__init__` 新增可选参数 `skill_root: Path | str | None = None`，默认 `state_root / "skills"`
  - `_run_task()` 在 `is_local_environment` 判断前插入 skill injection 逻辑
  - 当 `profile_snapshot.skills` 非空且 `self._skill_root` 目录存在时，调用 `SkillInjectionService.generate_ainrf()` 和 `sync_to_claude()`
- 新增 `tests/task_harness/test_launcher_integration.py`:
  - `test_build_local_launcher_prefers_ainrf_settings`: 验证 `.ainrf/settings.json` 优先于传入的 `settings_path`
  - `test_build_local_launcher_falls_back_to_settings_path`: 验证无 `.ainrf/settings.json` 时回退到传入路径
  - `test_build_local_launcher_no_settings`: 验证无 settings 时不含 `--settings` 参数
- 运行 `uv run pytest tests/task_harness/test_launcher_integration.py tests/test_task_harness_service.py` 全部 6 个测试通过
- `uv run ruff check` 和 `uv run ruff format` 无问题
- Commit: (待写入)

## 2026-05-06 15:20 CST — Task 11: Skill Import API endpoint (git + local)

- 修改 `src/ainrf/api/schemas.py`:
  - 新增 `SkillImportRequest`（source/git/local，url/local_path/skill_id）
  - 新增 `SkillImportResponse`（skill_id, label, path）
- 修改 `src/ainrf/api/routes/skills.py`:
  - 新增 `POST /skills/import` 端点
  - Git 来源：使用 `tempfile.mkdtemp` + `subprocess.run(git clone --depth 1)` 克隆到临时目录
  - Local 来源：`shutil.copytree` 复制本地目录
  - 读取 `skill.json` 获取原始 skill_id 和 label
  - 支持 `skill_id` override：重命名目标目录并更新 `skill.json`
  - 导入后验证 `skill.json` 和 `SKILL.md` 存在，否则清理并返回 400
  - 目标位置为 discovery service 的第一个 scan root
- 新增 `tests/api/test_skills.py` 5 个测试：
  - `test_import_skill_local_success`：本地目录导入成功
  - `test_import_skill_local_not_found`：不存在的本地路径返回 400
  - `test_import_skill_local_missing_manifest`：缺少 skill.json 返回 400
  - `test_import_skill_with_override`：skill_id override 重命名目录并更新 manifest
  - `test_import_skill_git_success`：使用 `file://` 协议克隆本地 git 仓库导入成功（skipif git 不可用）
- 运行 `uv run pytest tests/api/test_skills.py -v` 全部 9 个测试通过
- `uv run ruff check` 和 `uv run ruff format` 无问题
- Commit: `6715e23`

## 2026-05-06 17:20 CST — Task 15: End-to-end integration test

- 新增 `tests/test_skill_injection_e2e.py`：
  - 创建临时技能仓库（含 `skill.json`、`SKILL.md`、`scripts/search.py`）
  - 调用 `SkillInjectionService.generate_ainrf()` 生成 `.ainrf/` 目录
  - 调用 `sync_to_claude()` 同步到 `.claude/` 目录
  - 验证 `.ainrf/skills/web-search/` 目录及文件存在
  - 验证 `.ainrf/settings.json` 包含 `permissionMode: bypassPermissions` 和 `env.SEARCH_API_KEY`
  - 验证 `.ainrf/tool-manifest.json` 包含技能元数据、scripts 列表、mcp_servers 列表
  - 验证 `.claude/.ainrf-managed` marker 文件存在
  - 验证 `.claude/skills/` 存在（symlink 或目录）且包含技能文件
  - 验证 `.claude/settings.json` 已复制且内容正确
- `uv run pytest tests/test_skill_injection_e2e.py` 通过
- `uv run pytest tests/ --ignore=tests/test_api_tasks.py` 全部 182 个测试通过（`test_api_tasks.py` 中 `test_task_harness_remote_path_runs_without_readiness_precheck` 为 pre-existing 失败，与 skills 无关）
- `cd frontend && node_modules/.bin/tsc -b` 无错误
- `uv run ruff check src/ainrf/skills/ tests/skills/ src/ainrf/api/routes/skills.py tests/api/test_skills.py src/ainrf/task_harness/` 全部通过
- Commit: `8ee8d94`
