# 2026-03-26

- 13:35 CST | P7-adapter-real-path | 完成 P7 第一批落地：改造 `src/ainrf/agents/claude_code.py` 远程 runner，新增 `_invoke_with_sdk` 分发路径（优先尝试 `claude_code_sdk` 的 plan/execute 候选方法，失败自动回退），并补充 unknown action 错误响应；同时在 `tests/test_claude_code_adapter.py` 新增 2 条测试，锁定“非无条件 fallback”与 error 字段透传行为。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py` (5 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agents/claude_code.py tests/test_claude_code_adapter.py` (passed)
- 13:49 CST | P7-adapter-hardening | 完成 P7 第二批强化：1) `health_check` 改为轻量 SDK 导入探活，不再调用 `ensure_claude_code`；2) `_invoke` 增加异常分级映射（`CommandTimeoutError`/`TransferError`/`SSHConnectionError`/`JSONDecodeError` 标记 retryable）；3) 对 `project_dir` 相关 shell 命令加入 `shlex.quote`，降低路径注入风险；4) 远程 runner 的 SDK 调用兼容 awaitable 返回。并新增 4 条测试覆盖健康检查与 retryable/non-retryable 错误语义。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py -v` (9 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agents/claude_code.py tests/test_claude_code_adapter.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/agents/claude_code.py tests/test_claude_code_adapter.py` (passed)
- 15:07 CST | Claude Code Lucas gateway switch | 更新用户级 Claude Code 配置，移除异常的旧 `model` 值，改为通过 Lucas Anthropic-compatible 网关 `https://cliproxyapi.html.de5.net` 默认使用 `gpt-5.4`；同时补记全局配置切换与验证结果到当日 worklog。 | Validation: 直连 `POST https://cliproxyapi.html.de5.net/v1/messages` 成功返回 `model: gpt-5.4` 与 `OK`；`claude -p --no-session-persistence --output-format json 'Reply with exactly OK'` 成功，输出包含 `modelUsage.gpt-5.4`
- 13:53 CST | P7-engine-retryable | 完成 P7 引擎联动：在 `src/ainrf/engine/engine.py` 中让 `_advance_executing_task` 消费 `AgentExecutionError.retryable`，对可重试错误执行 pending queue 轮转并记录 `retry_count/max_retries/last_error`，超限后以 `agent_execution_retry_exhausted` 失败；不可重试错误维持立即失败。同步在 `tests/test_task_engine.py` 新增 2 条测试，覆盖“可重试错误后轮转继续完成”与“重试耗尽失败”。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_engine.py -v` (7 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py tests/test_task_engine.py -q` (16 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed)
- 14:06 CST | P8-initial-artifact-loop | 启动 P8 首批落地：在 `src/ainrf/engine/engine.py` 为 `run_baseline`/`run_full_experiment` 步骤新增 `ExperimentRun` 工件落库与 `experiment.started`/`experiment.completed` 事件发射；任务完成时自动汇总实验运行情况并生成 `QualityAssessment` 工件（含三维评分与 evidence 引用），同时将新工件引用写入任务 checkpoint。并在 `tests/test_task_engine.py` 新增 `test_run_once_produces_experiment_runs_and_quality_assessment` 覆盖 P8 关键产物链。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_engine.py -q` (8 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed)
- 14:11 CST | P8-deviation-diagnosis-evidence | 完成 P8 第二批落地：在 `src/ainrf/engine/engine.py` 为实验步骤新增偏差阈值检测（`expected_metrics` + `deviation_threshold_percent`），当偏差超阈值时自动落库 `EvidenceRecord(type=DEVIATION_ANALYSIS)` 并发射 `deviation.detected` / `diagnosis.completed` 事件；同时将偏差证据纳入 `QualityAssessment` 的 evidence 与评分汇总。并在 `tests/test_task_engine.py` 新增 `test_run_once_records_deviation_evidence_when_threshold_exceeded` 验证 P8-2 关键链路。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_engine.py -q` (9 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/engine/engine.py tests/test_task_engine.py` (passed)
- 14:21 CST | P8-final-atomic-steps | 完成 P8 收口：1) 在 `src/ainrf/agents/claude_code.py` 将 fallback 计划升级为 8 个 P8 原子步骤（`analyze_method` 至 `generate_quality_assessment`），并按 step kind 输出结构化 `step_updates`；2) 在 `src/ainrf/engine/engine.py` 新增对 `diagnose_deviation` / `compare_tables` 的结果消费并落库 `EvidenceRecord`，纳入任务工件引用；3) 在 `tests/test_claude_code_adapter.py` 与 `tests/test_task_engine.py` 新增测试覆盖 8-step 计划与分析证据链；4) 在 `docs/framework/v1-implementation-status.md` 将 P8 状态更新为已完成（注明 fallback 边界）。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py tests/test_task_engine.py -q` (21 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agents/claude_code.py src/ainrf/engine/engine.py tests/test_claude_code_adapter.py tests/test_task_engine.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/agents/claude_code.py src/ainrf/engine/engine.py tests/test_claude_code_adapter.py tests/test_task_engine.py` (passed)
- 14:32 CST | P9-mode1-discovery-closure | 完成 P9 最小闭环：1) `src/ainrf/engine/engine.py` 接入 Mode 1（`literature_exploration`）planning/executing 路径，不再 `mode_not_implemented` 失败；2) planning 阶段新增 `ExplorationGraph` 初始化/更新与计划关卡复用；3) executing 阶段新增 Mode 1 图谱更新、claim 落库、`check_termination` 终止收敛到 `generate_discovery_report`；4) `src/ainrf/agents/claude_code.py` fallback runner 新增 Mode 1 原子步骤与结构化 step_updates；5) 测试新增/替换 `tests/test_task_engine.py::test_run_once_executes_literature_exploration_mode` 与 `tests/test_claude_code_adapter.py::test_remote_runner_fallback_plan_contains_p9_atomic_steps`；6) 同步更新 `docs/framework/v1-implementation-status.md` 的 P9 状态与边界说明。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_engine.py tests/test_claude_code_adapter.py -q` (22 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/engine/engine.py src/ainrf/agents/claude_code.py tests/test_task_engine.py tests/test_claude_code_adapter.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/engine/engine.py src/ainrf/agents/claude_code.py tests/test_task_engine.py tests/test_claude_code_adapter.py` (passed)
- 14:57 CST | P9-mode1-hardening-and-ui | 完成 Mode 1 强化与可视化闭环：1) 将 `TaskMode` 规范值迁移到 `research_discovery` 并保留 `literature_exploration` 兼容解析；2) 在 `ExplorationGraph` 增加排序/剪枝/递归控制字段（`max_depth/max_breadth/max_no_claim_rounds/ranked_reference_ids/reference_scores/prune_reasons`），TaskEngine 增加 top-K 扩展、深度/收益递减终止与自动 explore 任务注入；3) WebUI 接入 `/tasks/{id}/artifacts` 并在 Run Detail 展示 Mode 1 图谱/Claim 明细；4) 增加 Mode 1 排序与递归测试、可选真实运行时 e2e smoke（按环境变量跳过）、并新增性能并发烟雾测试。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_engine.py tests/test_claude_code_adapter.py tests/test_performance_benchmarks.py -q -k 'not e2e_smoke_with_real_runtime'` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_webui_app.py -q -k 'not create_webui_returns_blocks'` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check ...` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests` (passed). Commits: `8d856bd feat: migrate mode1 naming and exploration controls`; `14f412f feat: render mode1 artifacts in webui run detail`; `8f7c71a test: expand mode1 coverage and add perf smoke`.
- 15:31 CST | P9-runtime-compat-observability | 完成 1/2/3/4 四项后续：1) API 同时暴露旧路径与 `/v1` 前缀兼容路由，补齐 `/v1/health` 免鉴权逻辑与回归测试；2) `ClaudeCode` 远程 runner 为 Mode1 关键步骤增加 SDK 优先标记（保留 fallback）；3) 真实运行时 e2e smoke 增加 preflight 失败分类（ssh/claude/api_key/project_dir）并验证当前环境下按预期 skip；4) WebUI Run Detail 增加 `no_new_claim_rounds`、`reference_scores`、`prune_reasons` 调试可视化；并新增实时运行烟雾清单文档。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py tests/test_api_auth.py tests/test_api_health.py -q` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py -q -k 'not e2e_smoke_with_real_runtime'` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py::test_claude_code_adapter_e2e_smoke_with_real_runtime -q` (skipped), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_webui_app.py -q -k 'not create_webui_returns_blocks'` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests` (passed). Commits: `8203f89 feat: add /v1 compatibility routes`; `c232b7d feat: prioritize mode1 sdk dispatch and e2e preflight`; `6495f19 feat: enrich mode1 run detail diagnostics in webui`.
- 15:39 CST | runtime-readme-authoring | 在 `src/ainrf/README.md` 新增运行时详细文档，覆盖模块职责、启动顺序（API/Worker/WebUI）、环境变量（API/MinerU/Container）、路由兼容策略（含 `/v1`）、状态目录结构、真实运行时 smoke 指引与常见排障，面向新成员快速上手与联调。 | Validation: 文档命令与变量名称逐项对照 `src/ainrf/cli.py`、`src/ainrf/api/config.py`、`src/ainrf/parsing/mineru.py`、`src/ainrf/execution/models.py` 与现有测试入口完成人工校验。
- 15:47 CST | cli-container-interactive-add | 增强 `ainrf` CLI：新增 `ainrf container add` 交互式容器配置命令，可从 SSH 命令解析 host/user/port/key，并支持输入 SSH 密码；配置写入 `.ainrf/config.json` 的 `container_profiles/default_container_profile`。同时扩展 `ContainerConfig` 与 `SSHExecutor` 支持密码认证（`AINRF_CONTAINER_PASSWORD` / asyncssh `password`），并让 `ApiConfig` 在未提供环境变量时可从 `config.json` 读取默认容器配置；补齐对应测试（CLI、execution、api config）与 README 说明。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py tests/test_execution_ssh.py tests/test_api_auth.py -q` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/cli.py src/ainrf/api/config.py src/ainrf/execution/models.py src/ainrf/execution/ssh.py tests/test_cli.py tests/test_execution_ssh.py tests/test_api_auth.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src tests` (passed).
- 16:06 CST | webui-container-profile-management | 在 WebUI Project Detail 新增“Container Profiles”能力：支持新增并保存容器配置（host/port/user/key/password/project_dir）到 `.ainrf/config.json`，可从下拉选择已保存 profile 预览，并一键回填到 Project Defaults 与 Run 表单容器字段；同时在 `JsonProjectStore`/`webui models` 增加容器 profile 持久化与类型模型，补充 store/app 测试覆盖保存、读取、应用流程。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format src/ainrf/webui/app.py src/ainrf/webui/models.py src/ainrf/webui/store.py tests/test_webui_store.py tests/test_webui_app.py` (3 reformatted), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/webui/app.py src/ainrf/webui/models.py src/ainrf/webui/store.py tests/test_webui_store.py tests/test_webui_app.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_webui_store.py tests/test_webui_app.py` (15 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` (passed).
- 16:16 CST | webui-default-container-profile | 增强 WebUI 容器配置流与 CLI 对齐：`JsonProjectStore.save_container_profile` 支持写入 `default_container_profile`，新增 `default_container_profile_name` 读取默认值；Project Detail 的 Saved Container 下拉默认指向当前默认 profile，并新增 “Set as default container profile” 选项接入保存回调。补充 WebUI/store 测试覆盖默认 profile 读写与保存校验错误路径（缺少 name/host/user）以及未选择 profile 的应用提示。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/webui/app.py src/ainrf/webui/store.py tests/test_webui_app.py tests/test_webui_store.py --output-format concise` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_webui_app.py tests/test_webui_store.py` (18 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check` (passed). Commit: `0313ca6 feat: support default container profile in webui`.
- 16:38 CST | api-serve-interactive-bootstrap | 修复首次启动 `ainrf serve` 因缺少 `api_key_hashes` 直接失败的问题：在 `src/ainrf/cli.py` 新增 `_ensure_api_key_hashes_configured`，当环境变量与 `.ainrf/config.json` 均未配置时，自动进入交互式 API key 输入并写入哈希到 `config.json`，同时覆盖前台与 daemon 启动路径；补充 `tests/test_cli.py` 两条交互式引导回归测试（前台/daemon），并更新 `src/ainrf/README.md` 说明首次无配置引导行为。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_cli.py -q` (15 passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/cli.py tests/test_cli.py --output-format concise` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/cli.py tests/test_cli.py` (passed). Commits: `91dd845 feat: bootstrap api key config on first serve`; `12ff37b docs: describe interactive serve bootstrap`.
- 16:52 CST | P7-skill-profile-adapter-foundation | 为 `ClaudeCodeAdapter` 增加受控 skill-inspired 适配层基础：新增 `src/ainrf/agents/skill_profiles.py` 定义 Mode 1/Mode 2 关键原子步骤的 skill profile、目标、输出键与 guidance，并在 `src/ainrf/agents/claude_code.py` 的 `execute_step` 请求中注入结构化 `skill` payload，供后续 runner / SDK 路径做 prompt 约束与 JSON 归一化；同时通过 `src/ainrf/agents/__init__.py` 导出 profile API，并在 `tests/test_claude_code_adapter.py` 补充 profile 映射、payload 构造与上传链路回归测试。 | Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_claude_code_adapter.py -q` (17 passed, 1 skipped), `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agents/claude_code.py src/ainrf/agents/skill_profiles.py src/ainrf/agents/__init__.py tests/test_claude_code_adapter.py` (passed), `UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/agents/claude_code.py src/ainrf/agents/skill_profiles.py tests/test_claude_code_adapter.py` (passed)
