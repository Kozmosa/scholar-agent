2026-04-21 00:00 changelog：完成 environments / containers 控制面闭环设计说明，明确先以执行环境为核心资源层推进五个增量 feat（API、Containers CRUD、手动探测、Terminal/Workspace 绑定、项目级引用/覆盖），并约束本阶段不切回 task runtime 主线；验证：已基于当前 worklog、最近提交、`src/ainrf/api/app.py`、`src/ainrf/environments/` 与 `tests/test_api_environments.py` 核对现状，设计与现有代码边界一致。
2026-04-21 00:00 changelog：完成 ttyd / Web terminal 收口设计，明确当前后端 ttyd 代理链路已基本完成，剩余工作应按两个提交推进：先打通 WebUI 真实 terminal API / iframe 打开链路，再整理 terminal 页面结构并补前端测试；验证：已结合最近终端相关提交、`src/ainrf/api/routes/terminal.py`、`frontend/src/api/client.ts`、`frontend/src/api/endpoints.ts`、`frontend/src/components/terminal/TerminalBenchCard.tsx` 与 `frontend/vite.config.ts` 核对现状，设计与当前代码边界一致。
2026-04-21 20:06 changelog：完成 ttyd / Web Terminal 收口实现，前端 API 统一注入 `X-API-Key` 并补充更可读的错误解析，`VITE_USE_MOCK` 切换改为显式开关，terminal 页面拆成 controller/view/hook 边界并保留 running 时 iframe 打开行为，同时新增 API、endpoint、terminal 组件与合同 smoke tests；验证：已执行 `npm run lint`、`npm run build` 和 `npm run test:run`（均通过）。
2026-04-21 20:22 changelog：修复 terminal iframe 的同源/第三方 cookie 问题，将前端 dev proxy 扩展到 `/terminal` 和 `/code`，并把 terminal iframe URL 归一化为前端同源路径，从而让 `/open -> /proxy` 链路可稳定携带 viewer session cookie；验证：已重跑 `npm run build` 和 `npm run test:run`（均通过）。
2026-04-21 20:23 changelog：继续收口 ttyd 打开链路，将 terminal 页面改为一次性 `open` + 后续同源 `proxy` 渲染，并用 `sessionStorage` 记忆已打开会话，避免刷新或重绘重复消耗 open token；验证：已重跑 `npm run build` 和 `npm run test:run`（均通过）。
2026-04-21 20:51 changelog：完成 terminal 从 ttyd/iframe 迁移到 `xterm.js + PTY/WebSocket` 的主实现，后端新增本地 PTY 会话管理与 WebSocket 数据通道，前端改为 xterm 终端画布并收口 start/stop 状态流，旧的 viewer/open/proxy 链路与相关测试已替换；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_terminal.py tests/test_terminal_pty.py`、`npm run lint`、`npm run build`、`npm run test:run` 通过，完整 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 仍存在仓库内与本次迁移无关的 `tests/test_api_environments.py` 失败。
2026-04-21 21:42 changelog：完成 environments 控制面第一层闭环接线，新增 `/environments` API router 并挂载到主 app 与 `/v1` 前缀，补齐 environment CRUD / detect / latest detection contract，前端新增 Containers 实页并接入环境列表、创建、编辑、激活、检测和删除交互，同时更新 dashboard 与 runtime README 的 environments 说明；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_environments.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`npm run lint`、`npm run build`、`npm run test:run`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/api src/ainrf/environments tests/test_api_environments.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过。
2026-04-21 21:55 changelog：完成 Terminal / Workspace environment selector 的 UI-first 接线，新增共享环境选择 hook 与 selector 面板，Dashboard 和 Workspaces 共用同一选择态并将其注入 terminal / workspace 入口，Terminal 启动前会校验是否已有有效 environment，选择结果通过本地存储记忆且在删除或失效后自动回退到可用项；验证：`npm run lint`、`npm run build`、`npm run test:run` 通过。
2026-04-21 22:24 changelog：完成前端 i18n 接入与中英文切换，新增本地语言上下文、默认语言检测与 `localStorage` 持久化、头部语言切换控件，并把 Layout、Dashboard、Workspaces、Containers、环境选择、终端、code-server、ErrorBoundary 与占位页文案统一改为双语；验证：`npm run lint`、`npm run test:run`、`npm run build` 通过。
2026-04-21 22:45 changelog：完成 Containers 默认 `localhost` 种子方案落地，新增共享 runtime 配置归一化与默认容器 profile 生成，onboarding / `container add` 自动补齐 `localhost` 与 `default_container_profile`，后端环境注册表内置不可删除的 `localhost` 种子项并让其探测失败时给出明确诊断，前端环境选择、Containers 页面、mock transport 与相关类型/文案同步支持默认项标记与删除锁定；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_auth.py tests/test_api_environments.py tests/test_cli.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/runtime/config.py src/ainrf/api/config.py src/ainrf/onboarding.py src/ainrf/cli.py src/ainrf/environments/models.py src/ainrf/environments/service.py src/ainrf/environments/__init__.py src/ainrf/api/routes/environments.py src/ainrf/api/schemas.py tests/test_api_auth.py tests/test_api_environments.py tests/test_cli.py`、`npm run test:run -- src/api/environments.test.ts src/components/environment/EnvironmentSelectorPanel.test.tsx src/components/terminal/TerminalBenchCard.test.tsx src/pages/ContainersPage.test.tsx`、`npm run lint`、`npm run build` 通过。
2026-04-21 22:51 changelog：完成 terminal 默认 shell 与外观微调，默认终端命令改为当前用户登录 shell（passwd 记录优先，其次回退 `SHELL`），减少了 `/bin/sh` 启动时的 job control 提示风险，同时把 terminal 面板与终端画布圆角收小；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_auth.py tests/test_api_terminal.py tests/test_terminal_pty.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/api/config.py tests/test_api_auth.py`、`npm run lint`、`npm run build` 通过。
2026-04-21 23:10 changelog：完成 Web Terminal 会话保活与用户绑定设计说明，明确采用“应用用户绑定远端 identity、每个 user×environment 固定 personal/agent 双 session、agent session 内多 task window、task 默认只读观察/显式接管暂停 agent/释放后自动恢复”的 V1 方案，并补齐生命周期、状态机、API、持久化与分阶段实施顺序；验证：已基于当前 terminal/api/environments 代码、既有控制面设计稿与交互确认完成 spec 自检，无占位符、无内部矛盾，范围已收敛到 tmux-first V1。
2026-04-21 23:30 changelog：完成 environments / containers 控制面 spec 完成度审计，逐项对照 `docs/superpowers/specs/2026-04-21-environments-containers-control-plane-design.md` 与现状实现，确认 environments API、Containers CRUD 与手动 Detect 已落地并通过测试，但 Terminal / Workspace 仍仅共享前端本地 environment 选择态，未把所选 environment 作为终端/code-server 的真实后端上下文，项目级 environment 引用/覆盖也仍停留在 service 内部能力，尚未形成 spec 所述第二层闭环；验证：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_environments.py tests/test_api_terminal.py tests/test_api_code.py`、`npm run test:run -- src/api/environments.test.ts src/components/environment/EnvironmentSelectorPanel.test.tsx src/components/terminal/TerminalBenchCard.test.tsx src/pages/ContainersPage.test.tsx` 通过。
