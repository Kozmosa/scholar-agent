2026-04-27 00:05 changelog：完成 Task 8/9/10 remaining robustness slices，补充 task stream SSE query-string API key 约束注释与测试，给 cwd seed workspace 添加短 WHY 注释；将 `api/app.py` router 注册改为同一 router tuple 的 root/`/v1` 循环并保持 lifespan 初始化顺序不变；新增 `src/ainrf/task_harness/artifacts.py` 统一 task harness 已知 artifact 文件名、JSON 写入/读取、prompt/binding/launch payload 持久化与 summary reload，`service.py` 与 remote launcher 复用该具体 helper，保持 artifact 文件名、JSON keys、detail response 与 launcher payload 语义不变；验证结果：`npm --prefix frontend run test:run -- ContainersPage endpoints` 通过（11 passed），`npm --prefix frontend run lint` 通过，`npm --prefix frontend run build` 成功；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py tests/test_task_harness_service.py` 通过（6 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。

2026-04-27 12:27 changelog：完成 feat/webui-task-enhance 的 Task + shell WebUI 重构切片；从 `origin/master` 创建 `.worktrees/feat-webui-task-enhance` worktree/branch，将 `/tasks` 改为 Kimi-style task sidebar + workspace，新增 URL query selection、sidebar search、create-task dialog、workspace output timeline/metadata panels，并刷新全局 Layout 与 CSS semantic tokens，同时强化 task SSE 切换时的输出清空、stream/timer 关闭与 stale event 防护；验证结果：`npm --prefix .worktrees/feat-webui-task-enhance/frontend test -- --run src/pages/TasksPage.test.tsx --reporter=dot` 通过（15 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run test:run` 通过（62 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run build` 成功，`uv run pytest` 通过（145 passed, 1 skipped）；`uv run ruff check .` 因 gitignored `ref-repos/EvoMaster` 既有 lint 问题失败，未修改该 reference repo。

2026-04-27 13:44 changelog：完成 feat/webui-task-enhance 的 task UI polish 修复切片；为 `/tasks` 增加可拖拽任务列表/inspect 分隔条并支持键盘微调，将 task 页面 sidebar、创建弹窗、详情面板与状态文案接入英/中文 i18n，同时把语言切换器改为 semantic token 配色，修复浅色 header 下近乎不可见的问题；验证结果：`npm --prefix .worktrees/feat-webui-task-enhance/frontend test -- --run src/i18n/LocaleSwitcher.test.tsx src/pages/TasksPage.test.tsx --reporter=dot` 通过（20 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run test:run` 通过（67 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run build` 成功。

2026-04-27 14:04 changelog：完成非 task 页面标题双语规则修复；将 Terminal、Workspaces、Containers、Settings 页面大标题统一为当前语言、小标题统一为备选语言，并补充页面级标题回归测试覆盖中英文切换；验证结果：`npm --prefix .worktrees/feat-webui-task-enhance/frontend test -- --run src/pages/TerminalPage.test.tsx src/pages/WorkspacesPage.test.tsx src/pages/ContainersPage.test.tsx src/pages/SettingsPage.test.tsx --reporter=dot` 通过（14 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run test:run` 通过（71 passed），`npm --prefix .worktrees/feat-webui-task-enhance/frontend run build` 成功。

2026-04-27 14:23 changelog：完成 AINRF fancy README 初稿；将根目录 `README.md` 从简短项目说明改写为中文为主、面向研究开发者的产品化 README，包含 badge hero、ASCII console preview、Why AINRF、Task Harness / Environment / Terminal / Workspace 能力说明、架构图、Quick Start、目录、技术栈、验证命令与文档入口；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build` 成功。

2026-04-27 17:56 changelog：完成 Terminal/Settings 环境选择器与个人终端会话文案修正；将终端页的共享环境选择器移动到设置页 General Preferences 与 Project Defaults 之间，终端页仅保留个人终端会话卡片；将终端会话卡片标题改为当前语言标题/备选语言小标题，并把说明文案改为卡片功能描述；验证结果：`npm --prefix frontend test -- --run src/pages/TerminalPage.test.tsx src/pages/SettingsPage.test.tsx src/components/terminal/TerminalBenchCard.test.tsx --reporter=dot` 通过（16 passed），`npm --prefix frontend run test:run` 通过（73 passed），`npm --prefix frontend run build` 成功。

2026-04-27 18:56 changelog：完成工作区管理器、工作区浏览器升格与终端手动附着切片；为 workspace registry/API 增加 create/update/delete 并保护默认工作区，将 `/workspaces` 改为工作区 CRUD 管理器，新增独立 `/workspace-browser` 页面承载既有 code-server 卡片与导航入口，同时移除终端页打开后的自动 reattach 行为，改为仅手动点击 Attach 后附着；验证结果：`npm --prefix frontend test -- --run src/pages/WorkspacesPage.test.tsx src/pages/WorkspaceBrowserPage.test.tsx src/components/terminal/TerminalBenchCard.test.tsx src/api/endpoints.test.ts --reporter=dot` 通过（21 passed），`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py` 通过（6 passed），`npm --prefix frontend run test:run` 通过（79 passed），`npm --prefix frontend run build` 成功，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest` 通过（147 passed, 1 skipped），`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` 通过，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 通过。
