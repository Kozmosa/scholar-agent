## 2026-04-30

### 15:30 — 完成 Skills 多选开关前端实现

- 新增 `SkillItem` / `SkillListResponse` 类型到 `frontend/src/types/index.ts`
- 新增 `getSkills()` API endpoint 到 `frontend/src/api/endpoints.ts`，`mockGetSkills()` 到 `frontend/src/api/mock.ts`
- 新增 `skills: string[]` 字段到 `ResearchAgentProfileSettings`，更新 defaults、storage（含向后兼容：从 `skillsPrompt` 按 `[,
]+` 分割解析）和 context
- 新建 `SkillToggleGroup` 胶囊开关组件（`frontend/src/components/ui/SkillToggleGroup.tsx`）
- 在 `TaskCreateForm` 中集成技能选择，提交时传递到 `research_agent_profile.skills`
- 在 `SettingsPage` 的 TaskConfigurationSection 中集成技能开关，skillsPrompt textarea 缩小为 `min-h-16`
- 更新 i18n messages（中英双语）
- 验证：TypeScript 零错误，前端构建成功，后端 209 tests passed

### 15:50 — 为剩余页面启用 SectionCard 折叠

- `SettingsPage`：GeneralPreferencesSection、Default Workspace、TaskConfigurationSection、ProjectDefaultsSection、EnvironmentDefaultsCard 全部启用 `collapsible`
- `TerminalBenchCardView`：终端控制台区域折叠（默认收起）
- `ContainersPage`：EnvironmentEditor、ProjectReferenceEditor 折叠（默认收起）
- `WorkspaceManagerCard`：编辑表单区域折叠
- 验证：TypeScript 零错误，前端构建成功
