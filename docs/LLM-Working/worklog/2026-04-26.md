2026-04-26 14:31 changelog：完成 Task 4 docs build cleanup，对 /scripts/build_html_notes.py 按职责拆分出 note 转换、处理结果收集、Markdown 输出与静态资源复制辅助函数，并补充 frontmatter 容错、仓库外 Markdown 链接降级、反向链接仅在构建产物追加的契约注释；验证结果：UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_build_html_notes.py 通过（19 passed），UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/build_html_notes.py build 成功，UV_CACHE_DIR=/tmp/uv-cache uv run ty check 通过，UV_CACHE_DIR=/tmp/uv-cache uv run ruff check scripts tests 通过，UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check scripts tests 通过。
2026-04-26 15:42 changelog：完成 Task 6 terminal console robustness 收尾，将 TerminalSessionConsole 的 WebSocket 生命周期恢复为组件内稳定实现，仅提取并保留终端 fit/resize 调度辅助 hook，移除未落地的 socket 抽取文件，保持 observe-only 输入拦截、open 后 resize 发送、状态/输出消息处理与 cleanup 不触发断开通知的既有行为；验证结果：frontend 执行 npm run test:run -- TerminalSessionConsole 通过（6 passed），npm run lint 通过，npm run build 成功。
